# [MCP] 注册资源模板

## 概述

MCP (Model Context Protocol) 提供了两种资源发现方式：

1. **直接资源** - 服务器直接暴露一组固定资源
2. **资源模板** - 服务器提供URI模板，客户端根据参数动态构造URI

## 直接资源

服务器通过 JSON-RPC 方法 `resources/list` 直接提供固定资源列表。

### 资源结构

```typescript
{
  uri: string;         // 资源的唯一 URI（例如 file:///xxx）
  name: string;        // 人类可读的名称
  description?: string;// 可选描述，解释用途或内容
  mimeType?: string;   // MIME 类型，如 text/plain, image/png
  size?: number;       // 文件大小（单位：字节）
}
```

### 示例

```json
{
  "uri": "file:///logs/build.log",
  "name": "构建日志",
  "description": "包含最近一次构建的所有输出信息",
  "mimeType": "text/plain",
  "size": 18423
}
```

## 资源模板

服务器提供一组 URI 模板，供客户端根据参数动态构造 URI（例如选择城市、文件名等）。

### RFC 6570 URI 模板

模板符合 [RFC 6570](https://datatracker.ietf.org/doc/html/rfc6570) 标准，例如：

- `file:///project/src/{filename}`
- `screen://localhost/{displayId}`

### 模板结构

```typescript
{
  uriTemplate: string; // 可变 URI 模板（如 file:///{path}）
  name: string;        // 模板的说明名称
  description?: string;// 模板描述
  mimeType?: string;   // 匹配资源的 MIME 类型（适用于所有匹配项）
}
```

### 示例

```json
{
  "uriTemplate": "file:///home/user/{filename}",
  "name": "用户目录下的文件",
  "description": "允许读取任意用户目录下的文件名",
  "mimeType": "text/plain"
}
```

## MCP Server 实现

### 基础配置

```javascript
import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import {
  ListResourcesRequestSchema,
  ReadResourceRequestSchema,
  ListResourceTemplatesRequestSchema,
} from "@modelcontextprotocol/sdk/types.js";

const server = new Server(
  {
    name: "resources-server",
    version: "0.1.0",
    description: "提供资源的MCP服务器",
  },
  {
    capabilities: {
      resources: {},
    },
  }
);
```

### 请求处理器

| 功能 | Schema 名 | 请求结构 |
|------|-----------|----------|
| 读取资源 | `ReadResourceRequestSchema` | `{ method: "resources/read", params: { uri } }` |
| 列出资源 | `ListResourcesRequestSchema` | `{ method: "resources/list", params: {} }` |
| 列出资源模板 | `ListResourceTemplatesRequestSchema` | `{ method: "resources/templates", params: {} }` |

### 列出直接资源

```javascript
server.setRequestHandler(ListResourcesRequestSchema, async () => {
  return {
    resources: [
      {
        uri: "bananaphone://info",
        name: "香蕉手机信息",
        description: "香蕉手机的产品介绍和特性",
        mimeType: "text/plain",
      },
      {
        uri: "image://books",
        name: "图书图片",
        description: "books.jpeg图片文件",
        mimeType: "image/jpeg",
      },
    ],
  };
});
```

### 列出资源模板

```javascript
server.setRequestHandler(ListResourceTemplatesRequestSchema, async () => {
  return {
    resourceTemplates: [
      {
        uriTemplate: "code://file/{filename}",
        name: "代码文件",
        description: "访问code目录下的JS文件的内容",
        mimeType: "text/javascript",
      },
    ],
  };
});
```

### 读取资源（统一处理器）

```javascript
server.setRequestHandler(ReadResourceRequestSchema, async (request) => {
  const { uri } = request.params;
  console.error("uri>>>", uri);

  // 处理文本资源
  if (uri === "bananaphone://info") {
    const content = await readBananaPhoneInfo("src/assets", "bananaphone.txt");
    return {
      contents: [
        {
          uri: uri,
          mimeType: "text/plain",
          text: content,
        },
      ],
    };
  }

  // 处理图片资源（base64编码）
  if (uri === "image://books") {
    const base64Data = await readBinaryFile("src/assets", "books.jpeg");
    return {
      contents: [
        {
          uri: uri,
          mimeType: "image/jpeg",
          text: base64Data,
        },
      ],
    };
  }

  // 处理资源模板（动态URI）
  if (uri.startsWith("code://file/")) {
    const uriObj = new URL(uri);
    const filename = uriObj.pathname.split("/").pop();

    if (!filename || !filename.endsWith(".js")) {
      throw new Error("请指定一个有效的.js文件");
    }

    const content = await readCodeFile(filename);
    return {
      contents: [
        {
          uri,
          mimeType: "text/javascript",
          text: content,
        },
      ],
    };
  }

  throw new Error(`未知的资源URI: ${uri}`);
});
```

## 工具函数实现

### 读取文本文件

```javascript
export async function readBananaPhoneInfo(filepath, filename) {
  try {
    const filePath = path.join(process.cwd(), filepath, filename);
    const content = fs.readFileSync(filePath, "utf8");
    return content;
  } catch (error) {
    return `读取香蕉手机信息失败: ${error.message}`;
  }
}
```

### 读取代码文件

```javascript
export async function readCodeFile(filename) {
  try {
    const filePath = path.join(process.cwd(), "src/code", filename);
    const content = fs.readFileSync(filePath, "utf8");
    return content;
  } catch (error) {
    return `读取代码文件失败: ${error.message}`;
  }
}
```

### 读取二进制文件（图片等）

```javascript
export async function readBinaryFile(filepath, filename) {
  try {
    const filePath = path.join(process.cwd(), filepath, filename);
    const buffer = fs.readFileSync(filePath);
    // 将二进制数据转换为base64编码
    const base64Data = buffer.toString("base64");
    return base64Data;
  } catch (error) {
    throw new Error(`读取二进制文件失败: ${error.message}`);
  }
}
```

## 关键要点

1. **统一读取接口**：无论是直接资源还是资源模板，都使用 `resources/read` 方法读取
2. **URI 处理**：在读取处理器中需要自行判断URI类型并做相应处理
3. **二进制数据**：图片等二进制资源需要转换为base64编码传输
4. **动态URI**：资源模板允许客户端通过参数动态构造URI，提供更灵活的资源访问

## 课堂示例项目结构

```
demo/
├── package.json
└── src/
    ├── server.js          # MCP服务器主文件
    ├── utils.js           # 工具函数
    ├── assets/
    │   ├── bananaphone.txt # 文本资源
    │   └── books.jpeg     # 图片资源
    └── code/
        ├── 1.js           # 代码文件示例1
        └── 2.js           # 代码文件示例2
```

## 依赖配置

```json
{
  "name": "demo",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.16.0"
  }
}
```

## 启动服务器

```javascript
const transport = new StdioServerTransport();
await server.connect(transport);
```
