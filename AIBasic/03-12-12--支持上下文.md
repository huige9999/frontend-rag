# 第12课：支持上下文

## 课程概述

本课程介绍如何为大模型对话应用添加上下文支持功能，使AI能够记住之前的对话内容，提供更连贯的对话体验。

## 核心原理

支持上下文的背后原理非常简单：使用一个数组来存储会话的历史记录，每次请求时将历史会话记录一同发送给大模型。

## 关键代码实现

### 1. 服务端实现 (Node.js/Express)

#### 会话存储结构
```javascript
const conversations = [];
/**
 * conversations = [
 *  {role: "user", content: "你是谁"},
 *  {role: "assistant", content: "大模型的回复"},
 * ]
 */
```

#### API接口实现

**对话接口 (POST /ask)**
```javascript
router.post("/ask", async (req, res) => {
  // 拿到用户的问题
  const question = req.body.question || "";

  // 每一次 prompt 会将历史会话带过去
  const prompt = [
    "你是一个中文智能助手，请使用中文回答用户的问题。",
    // 历史记录
    ...conversations.map(
      (item) => `${item.role === "user" ? "用户" : "助手"}：${item.content}`
    ),
    `用户的问题：${question}`,
  ].join("\n");

  const response = await fetch("http://localhost:11434/api/generate", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      model: "llama3",
      prompt,
      stream: true,
    }),
  });

  // 设置SSE流响应头
  res.setHeader("Content-Type", "text/event-stream");
  res.setHeader("Cache-Control", "no-cache");

  const reader = response.body.getReader();
  const decoder = new TextDecoder("utf-8");
  let fullResponse = "";

  while (true) {
    const { done, value } = await reader.read();
    if (done) break;

    const chunk = decoder.decode(value, { stream: true });
    const lines = chunk.split("\n").filter((line) => line.trim());

    for (const line of lines) {
      try {
        const data = JSON.parse(line);
        if (data.response) {
          fullResponse += data.response;
          res.write(`${JSON.stringify({ response: data.response })}\n`);
        }
      } catch (e) {
        console.error("JSON解析失败", e.message);
      }
    }
  }

  // 将这一次会话记录到历史数组里面
  conversations.push(
    { role: "user", content: question },
    { role: "assistant", content: fullResponse }
  );

  // 限制会话历史的长度，仅保留最近的 20 条消息
  if (conversations.length > 20)
    conversations.splice(0, conversations.length - 20);

  res.end();
});
```

**获取历史记录接口 (GET /history)**
```javascript
router.get("/history", function (req, res) {
  res.json({
    conversations,
  });
});
```

**清空会话记录接口 (POST /clear)**
```javascript
router.post("/clear", function (req, res) {
  conversations.length = 0;
  res.json({
    message: "对话历史已经清空",
  });
});
```

### 2. 客户端实现 (Vue 3 + TypeScript)

#### 核心状态管理
```typescript
const input = ref(""); // 输入框内容
const chatBox = ref<HTMLElement | null>(null); // 聊天框DOM引用
const messages = ref<{role: 'user' | 'bot'; text:string}[]>([]); // 消息列表
const loading = ref(false); // 加载状态
```

#### 发送消息函数
```typescript
async function handleSubmit(){
  const question = input.value.trim();
  if(!question) return;

  // 添加用户消息
  messages.value.push({
    role: 'user',
    text: question
  });
  
  input.value = "";
  loading.value = true;

  await nextTick();
  
  // 滚动到底部
  chatBox.value?.scrollTo({
    top: chatBox.value.scrollHeight,
    behavior: 'smooth'
  })

  // 发送请求到代理服务器
  const res = await fetch("/api/ask", {
    method: "POST",
    headers: { 'Content-Type': "application/json"},
    body: JSON.stringify({question})
  });

  // 处理流式响应
  const reader = res.body?.getReader();
  if(!reader){
    console.error("响应流为空");
    loading.value = false;
    return;
  }

  const decoder = new TextDecoder("utf-8");
  let botMessage = "";
  const newMessage = reactive({role: 'bot' as const, text: ''});
  messages.value.push(newMessage);

  while(true){
    const {done, value} = await reader.read();
    if(done) break;

    const chunk = decoder.decode(value, { stream: true});
    const lines = chunk.split("\n").filter(line=>line.trim());

    for(const line of lines){
      try{
        const data = JSON.parse(line);
        if(data.response){
          if(loading.value){
            loading.value = false;
          }
          botMessage += data.response;
          newMessage.text = botMessage;
        }
      }catch(e){
        console.error("JSON解析失败", e);
      }
    }
  }

  await nextTick();
  chatBox.value?.scrollTo({
    top: chatBox.value.scrollHeight,
    behavior: 'smooth'
  })
}
```

#### 历史记录管理
```typescript
// 查看历史记录
async function showHistory(){
  try{
    const response = await fetch('/api/history')
    if(response.ok){
      const data = await response.json();
      historyData.value = data.conversations;
      showHistoryModal.value = true;
    } else {
      alert("获取历史记录失败")
    }
  }catch(error){
    alert("获取历史记录失败，请检查网络")
  }
}

// 清空历史记录
async function clearHistory(){
  if(!confirm("确定是否要清除所有的会话历史记录？该操作不可逆")) return
  
  try{
    const response = await fetch("/api/clear", {
      method: "POST",
      headers: { 'Content-Type': 'application/json' }
    })
    if(response.ok) {
      messages.value = [];
      alert("对话历史已删除")
    }
    else alert("清除失败，请稍候再试")
  }catch(error){
    alert("清除失败，请检查网络")
  }
}
```

## 技术要点

### 1. 上下文管理策略
- **内存存储**：使用数组存储会话历史
- **长度限制**：保留最近20条消息，避免token超限
- **消息格式**：统一使用 `{role, content}` 结构

### 2. 流式响应处理
- **SSE协议**：使用 `text/event-stream` 内容类型
- **分块传输**：边接收边显示，提升用户体验
- **JSON解析**：逐行解析流式数据

### 3. 用户体验优化
- **自动滚动**：新消息自动滚动到底部
- **加载状态**：显示"正在思考..."动画
- **历史管理**：支持查看和清空历史记录

## 项目结构

```
chat-bot/
├── client/              # Vue 3 前端
│   ├── src/
│   │   ├── App.vue     # 主组件
│   │   ├── main.ts     # 入口文件
│   │   └── assets/     # 静态资源
│   └── package.json
└── server/              # Node.js 后端
    ├── app.js          # Express应用配置
    ├── bin/
    │   └── www         # 服务器启动文件
    └── routes/
        └── index.js    # 路由处理
```

## 环境要求

- **后端**：Node.js + Express
- **前端**：Vue 3 + TypeScript + Vite
- **大模型**：Ollama (llama3模型)

## 运行说明

### 后端启动
```bash
cd server
npm install
npm start
```

### 前端启动
```bash
cd client
npm install
npm run dev
```

## 核心概念总结

1. **上下文保持**：通过维护对话历史数组实现
2. **内存管理**：限制历史长度，平衡记忆与成本
3. **流式处理**：SSE协议实现实时响应
4. **状态同步**：前后端消息状态保持一致
5. **用户控制**：提供历史查看和清空功能

## 扩展思路

- 可以将对话历史持久化到数据库
- 实现对话总结功能，进一步压缩上下文
- 添加用户身份识别，支持多用户会话隔离
- 实现对话导出和分享功能
