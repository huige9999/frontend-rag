# 获取实时信息 Part 2

## 课程概述

本课程介绍如何让大语言模型获取实时信息，通过外部工具扩展模型能力。

## 核心概念

### 工具调用原理
大模型本身无法直接使用工具，而是返回"我要使用 XXX 工具"的信息，由外部系统解析并执行相应工具调用。

### 工具箱传递方式
- **早期方案**：通过提示词（Prompt）方式传递工具信息
- **现代方案**：通过结构化工具定义和函数调用

## 项目结构

```
chat-bot/
├── client/              # 前端客户端（Vue.js）
│   ├── src/
│   │   ├── App.vue     # 主应用组件
│   │   └── main.ts     # 入口文件
├── server/             # 后端服务器（Express.js）
│   ├── app.js          # Express应用配置
│   ├── routes/
│   │   └── index.js    # API路由定义
│   ├── utils/
│   │   ├── LLM.js      # 大模型调用封装
│   │   ├── translateHandler.js  # 翻译工具
│   │   └── wetherHandler.js     # 天气工具
│   └── .env            # 环境变量配置
```

## 核心功能实现

### 1. 翻译工具（百度翻译API）

**功能**：支持中文到英文的翻译

**实现要点**：
```javascript
// translateHandler.js
async function translate({ input }) {
  // 提取需要翻译的文本
  const text = input.replace(/.*翻译.*?[：:]?\s*/, "").trim();
  
  // 生成签名（百度翻译API要求）
  const salt = Date.now();
  const sign = crypto
    .createHash("md5")
    .update(ID + text + salt + KEY)
    .digest("hex");
  
  // 调用百度翻译API
  const url = `https://fanyi-api.baidu.com/api/trans/vip/translate?q=${encodeURIComponent(text)}&from=zh&to=en&appid=${ID}&salt=${salt}&sign=${sign}`;
  
  const res = await fetch(url);
  const data = await res.json();
  
  if (data.trans_result?.length > 0) {
    return `翻译结果：${data.trans_result[0].dst}`;
  }
}
```

### 2. 天气工具（和风天气API）

**功能**：查询指定城市、指定日期的天气情况

**实现要点**：
```javascript
// wetherHandler.js

// 日期格式化
function formatDate(text) {
  const today = new Date();
  
  if (text.includes("今天")) return today.toISOString().split("T")[0];
  if (text.includes("明天")) {
    const tomorrow = new Date(today.getTime() + 86400000);
    return tomorrow.toISOString().split("T")[0];
  }
  if (text.includes("后天")) {
    const dayAfter = new Date(today.getTime() + 2 * 86400000);
    return dayAfter.toISOString().split("T")[0];
  }
  
  return null;
}

// 获取城市位置ID
async function getCityLocation(city) {
  const url = `https://geoapi.qweather.com/v2/city/lookup?location=${encodeURIComponent(city)}&key=${HEFENG_API_KEY}`;
  const res = await fetch(url);
  const data = await res.json();
  
  if (data.code === "200" && data.location?.length > 0) {
    return data.location[0].id;
  }
  return null;
}

// 查询天气
async function getWether({ city, date }) {
  const formattedDate = formatDate(date);
  const locationId = await getCityLocation(city);
  
  const url = `https://devapi.qweather.com/v7/weather/7d?location=${locationId}&key=${HEFENG_API_KEY}`;
  const res = await fetch(url);
  const data = await res.json();
  
  const match = data.daily.find((d) => d.fxDate === formattedDate);
  
  return `📍 ${city}（${formattedDate}）天气：${match.textDay}，气温 ${match.tempMin}°C ~ ${match.tempMax}°C`;
}
```

### 3. 大模型调用封装

**功能**：提供统一的LLM调用接口，支持流式和非流式响应

**实现要点**：
```javascript
// LLM.js

// 带超时机制的fetch
async function fetchWithTimeout(url, options = {}, timeout = LLM_TIMEOUT_MS) {
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), timeout);
  
  try {
    const response = await fetch(url, {
      ...options,
      signal: controller.signal,
    });
    
    clearTimeout(timeoutId);
    return response;
  } catch (err) {
    clearTimeout(timeoutId);
    if (err.name === "AbortError") {
      throw new Error("请求超时，请稍候重试");
    }
    throw err;
  }
}

// LLM调用主函数
async function callLLM({ prompt, stream = false, callback }) {
  const response = await fetchWithTimeout(LLM_ENDPOINT, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      model: LLM_MODEL,
      prompt,
      stream,
    }),
  });
  
  if (!stream) {
    const data = await response.json();
    return data.response;
  }
  
  // 流式处理
  const reader = response.body.getReader();
  const decoder = new TextDecoder("utf-8");
  let fullResponse = "";
  
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    
    const chunk = decoder.decode(value, { stream: true });
    const lines = chunk.split("\n").filter((line) => line.trim());
    
    for (const line of lines) {
      const data = JSON.parse(line);
      if (data.response) {
        fullResponse += data.response;
        callback?.(data.response);
      }
    }
  }
  
  return fullResponse;
}
```

### 4. API路由实现

**主要接口**：
- `POST /ask` - 处理用户问答
- `GET /history` - 获取会话历史
- `POST /clear` - 清空会话记录

**会话管理**：
```javascript
// routes/index.js
const conversations = [];

router.post("/ask", async (req, res) => {
  const question = req.body.question || "";
  
  // 构建包含历史记录的prompt
  const prompt = [
    "你是一个中文智能助手，请使用中文回答用户的问题。",
    ...conversations.map(
      (item) => `${item.role === "user" ? "用户" : "助手"}：${item.content}`
    ),
    `用户的问题：${question}`,
  ].join("\n");
  
  // 设置SSE流式响应
  res.setHeader("Content-Type", "text/event-stream");
  res.setHeader("Cache-Control", "no-cache");
  
  // 处理流式响应
  const reader = response.body.getReader();
  // ... 流式处理逻辑
  
  // 保存会话记录
  conversations.push(
    { role: "user", content: question },
    { role: "assistant", content: fullResponse }
  );
  
  // 限制历史记录长度
  if (conversations.length > 20) {
    conversations.splice(0, conversations.length - 20);
  }
});
```

## 环境配置

**必需的环境变量**：
```env
LLM_ENDPOINT=http://localhost:11434/api/generate
LLM_MODEL=llama3
LLM_TIMEOUT_MS=30000
HEFENG_API_KEY=your_hefeng_api_key
BAIDU_APP_ID=your_baidu_app_id
BAIDU_SECRET_KEY=your_baidu_secret_key
```

## 技术要点

1. **工具调用模式**：模型返回工具调用意图，系统解析并执行
2. **流式响应**：使用SSE (Server-Sent Events) 实现实时流式输出
3. **会话管理**：维护会话历史，支持上下文对话
4. **错误处理**：超时机制、异常捕获、友好错误提示
5. **API集成**：标准化第三方API调用流程

## 外部工具集成示例

### 支持的外部工具
1. **翻译服务**：百度翻译API
2. **天气服务**：和风天气API

### 集成流程
1. 解析用户意图，确定需要调用的工具
2. 提取工具调用所需的参数
3. 调用第三方API获取数据
4. 将结果格式化后返回给用户

## 总结

通过本课程学习，掌握了：
- 如何让大模型获取实时信息的方案设计
- 第三方API的集成方法
- 流式响应的实现技术
- 会话管理的最佳实践
- 工具调用模式的应用场景

这种工具调用的方式为后续学习Function Calling奠定了基础，是现代AI应用开发的重要模式。
