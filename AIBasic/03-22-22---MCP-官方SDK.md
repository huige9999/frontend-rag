# 22. [MCP]官方SDK

## 课时概述

本课程深入讲解MCP (Model Context Protocol) 理论知识和官方SDK的使用，涵盖进程通信、MCP协议规范以及如何开发MCP服务器。

## 核心知识点

### 1. stdio 进程通信

#### 基本概念
- **进程**: 执行应用程序后启动的进程，操作系统为其分配内存空间和系统资源
- **进程监听**: 进程通过监听保持运行状态（如微信、QQ）
- **父子进程模型**: 一个进程可以启动另一个进程

#### 标准输入输出
每个进程启动后都有两个对外通信接口:
- **标准输入接口**: standard in
- **标准输出接口**: standard output

#### 实现示例
```javascript
// 监听输入，保持进程不结束
process.stdin.on("data", () => {});
```

#### 父子进程通信示例
```javascript
const { spawn } = require("child_process");

// 启动 server.js 子进程
const serverProcess = spawn("node", ["server.js"]);

// 监听服务端的响应
serverProcess.stdout.on("data", (data) => {
    process.stdin.write(data.toString());
});

// 发送测试消息
const messages = ["生命有意义吗？", "宇宙有尽头吗？", "再见！"];

messages.forEach((msg, index) => {
  setTimeout(() => {
    console.log(`-->${msg}`);
    serverProcess.stdin.write(msg);
  }, index * 1000);
});
```

**特点**: stdio通信高效、简洁，但仅适用于本地进程间通信

### 2. 通信格式

#### JSON-RPC 2.0
JSON Remote Procedure Call（远程函数调用）

**请求格式**:
```json
{
  "jsonrpc": "2.0",
  "method": "sum",
  "params": {
    "a": 5,
    "b": 6
  },
  "id": 1
}
```

**响应格式**:
```json
{
  "jsonrpc": "2.0",
  "result": 11,
  "id": 1
}
```

### 3. MCP Server 协议

MCP是一套标准协议，规定应用程序之间如何通信:

#### 通信方式
- **stdio**: 推荐，高效、简洁、本地
- **http**: 可远程
  - StreamHTTP
  - SSE

#### 通信格式
基于JSON-RPC的进一步规范

#### 基本规范

**1. 初始化 initialize**

**请求**:
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2024-11-05",
    "capabilities": {
      "roots": {
        "listChanged": true
      },
      "sampling": {},
      "elicitation": {}
    },
    "clientInfo": {
      "name": "ExampleClient",
      "title": "Example Client Display Name",
      "version": "1.0.0"
    }
  }
}
```

**响应**:
```json
{
  "jsonrpc": "2.0",
  "id": 1, 
  "result": {
    "protocolVersion": "2024-11-05",
    "capabilities": {
      "logging": {},
      "prompts": {
        "listChanged": true
      },
      "resources": {
        "subscribe": true,
        "listChanged": true
      },
      "tools": {
        "listChanged": true
      }
    },
    "serverInfo": {
      "name": "ExampleServer",
      "title": "Example Server Display Name",
      "version": "1.0.0"
    },
    "instructions": "Optional instructions for the client"
  }
}
```

**2. 发现工具 tools/list**

**请求**:
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/list"
}
```

**响应**:
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "tools": [
      {
        "name": "get_weather",
        "title": "Weather Information Provider",
        "description": "Get current weather information for a location",
        "inputSchema": {
          "type": "object",
          "properties": {
            "location": {
              "type": "string",
              "description": "City name or zip code"
            }
          },
          "required": ["location"]
        }
      }
    ]
  }
}
```

**3. 工具调用 tools/call**

**请求**:
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/call",
  "params": {
    "name": "get_weather",
    "arguments": {
      "location": "New York" 
    }
  }
}
```

**响应**:
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "content": [
      {
        "type": "text", 
        "text": "72°F" 
      }
    ]
  }
}
```

#### 调试工具
```bash
npx @modelcontextprotocol/inspector
```

### 4. 官方SDK使用

#### 安装
```bash
npm install @modelcontextprotocol/sdk
```

#### 实际示例

**package.json**:
```json
{
  "name": "demo",
  "type": "module",
  "scripts": {
    "start": "node ./src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.16.0",
    "zod": "^3.25.76"
  }
}
```

**server.js 完整代码**:
```javascript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { z } from "zod";
import fs from "fs";

const server = new McpServer({
  name: "my mcp server",
  version: "0.1.0",
});

// 注册一个工具
server.registerTool(
  "sum", // 工具名称
  {
    title: "两数求和",
    description: "得到两个数的和",
    inputSchema: {
      a: z.number().describe("第一个数"),
      b: z.number().describe("第二个数"),
    },
  },
  ({ a, b }) => {
    // 正常的工具（函数）的逻辑
    return {
      content: [
        {
          type: "text",
          text: `两个数的和为${a + b}`,
        },
      ],
    };
  }
);

server.registerTool(
  "createFile",
  {
    title: "创建文件",
    description: "在指定目录下创建一个文件",
    inputSchema: {
      filename: z.string().describe("文件名"),
      content: z.string().describe("文件内容"),
    },
  },
  ({ filename, content }) => {
    try {
      fs.writeFileSync(filename, content);
      return {
        content: [
          {
            type: "text",
            text: `文件创建成功！`,
          },
        ],
      };
    } catch (err) {
      return {
        content: [
          {
            type: "text",
            text: err.message || "文件创建失败！",
          },
        ],
      };
    }
  }
);

const transport = new StdioServerTransport(); // 创建一个 stdio 的传输通道
server.connect(transport); // 进行连接
```

### 5. 对接AI应用

#### AI应用程序定义
所有能与大模型交互的应用都可以看作是AI应用程序:
- ChatGPT
- DeepSeek Chat Page
- Claude Desktop
- VSCode
- Cursor
- ...

#### MCP支持的应用
- **Claude Desktop**: https://claude.ai/download
- **Cursor**: https://cursor.com/cn

#### 工作流程

**传统流程**:
用户和AI应用交互 → AI应用调用大模型 → 返回结果

**MCP增强流程**:
1. 用户和AI应用交互
2. AI应用调用大模型
3. 大模型判断需要调用工具
4. AI应用调用MCP工具
5. 将工具结果返回给大模型
6. 大模型生成最终回复

#### 核心概念

- **MCP Host**: 往往指代AI应用本身，用于发现MCP Server以及其中的工具列表
- **MCP Client**: 用于和MCP Server通信的客户端，往往在Host内部开启

#### Claude Desktop 工作流程示例
1. 在Claude Desktop中打开新的聊天窗口
2. Claude查看当前启用了哪些MCP Server
3. Claude（host）为每个MCP Server创建一个Client
4. 每个Client分别启动各自的MCP Server，进行初始化和工具列表通信
5. 当需要调用工具时，每个Client负责调用各自的工具并把结果传递给Host
6. Host根据结果处理后续逻辑

## 课程资料

- **课件资料**: MCP理论知识.md
- **课堂代码**: demo项目
  - 使用官方SDK实现MCP服务器
  - 包含工具注册示例（求和、文件创建）

## 技术要点

1. **进程间通信**: 理解stdio通信机制和父子进程模型
2. **协议规范**: 掌握MCP协议的初始化、工具发现和工具调用流程
3. **SDK使用**: 使用`@modelcontextprotocol/sdk`简化MCP服务器开发
4. **实际应用**: 理解MCP如何扩展AI应用的能力边界

## 依赖包

- `@modelcontextprotocol/sdk`: MCP官方SDK
- `zod`: 参数验证库