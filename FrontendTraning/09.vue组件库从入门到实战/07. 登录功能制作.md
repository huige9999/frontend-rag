# 07. 登录功能制作

## 课程概述

本节课程实现登录功能的核心逻辑，包括前后端接口对接、用户认证、Token 管理、错误处理等，完成完整的登录流程。

## 登录流程概述

### 1. 用户登录流程图

```
用户输入信息 → 表单验证 → 发送登录请求 → 服务器验证 → 返回结果
                                           ↓
                                    验证成功 ← 验证失败
                                           ↓
                            存储用户信息    显示错误信息
                            跳转到首页      刷新验证码
```

### 2. 服务器返回数据后的流程

```
接收响应数据 → 提取用户信息和 Token → 存储到 Vuex → 存储到本地存储
                                                    ↓
                                            跳转到首页/重定向页面
```

## API 接口封装

### 1. 用户登录接口

`src/api/user.js`：

```javascript
import request from '@/utils/request'

// 登录接口
export function loginApi(data) {
  return request({
    url: '/api/admin/login',
    method: 'post',
    data
  })
}

// 获取用户信息
export function getInfo(token) {
  return request({
    url: '/vue-admin-template/user/info',
    method: 'get',
    params: { token }
  })
}

// 退出登录
export function logout() {
  return request({
    url: '/vue-admin-template/logout',
    method: 'post'
  })
}
```

### 2. 接口参数说明

**登录接口参数：**

```javascript
{
  loginId: 'admin',      // 管理员账号
  loginPwd: '123456',    // 登录密码
  captcha: 'abc123',     // 验证码
  remember: 7            // 记住登录天数（可选）
}
```

**登录接口响应：**

```javascript
{
  data: {
    _id: 'xxx',
    loginId: 'admin',
    nickname: '管理员',
    // ...其他用户信息
  }
}
```

## 请求拦截器配置

### 1. Token 自动注入

`src/utils/request.js`：

```javascript
import axios from 'axios'
import { MessageBox, Message } from 'element-ui'
import store from '@/store'

// 创建 axios 实例
const service = axios.create({
  timeout: 5000
})

// 请求拦截器
service.interceptors.request.use(
  config => {
    // 在发送请求之前做些什么
    if (store.getters.token) {
      // 让每个请求携带 token
      config.headers['X-Token'] = getToken()
    }
    return config
  },
  error => {
    // 请求错误处理
    console.log(error)
    return Promise.reject(error)
  }
)
```

### 2. 响应拦截器 - Token 处理

```javascript
// 响应拦截器
service.interceptors.response.use(
  response => {
    // 检查响应头中是否包含 authentication token
    if (response.headers.authentication) {
      // 将 token 存储到 localStorage
      localStorage.adminToken = response.headers.authentication;
    }
    return response.data
  },
  error => {
    // 响应错误处理
    console.log('err' + error)
    Message({
      message: error.message,
      type: 'error',
      duration: 5 * 1000
    })
    return Promise.reject(error)
  }
)

export default service
```

## Vuex 状态管理

### 1. User Store 模块

`src/store/modules/user.js`：

```javascript
import { loginApi, logout, getInfo } from '@/api/user'
import { getToken, setToken, removeToken } from '@/utils/auth'
import { resetRouter } from '@/router'

const getDefaultState = () => {
  return {
    user: null  // 存储登录后的用户信息
  }
}

const state = getDefaultState()

const mutations = {
  SET_USER: (state, payload) => {
    state.user = payload
  }
}

const actions = {
  // 用户登录
  login({ commit }, userInfo) {
    return new Promise((resolve, reject) => {
      loginApi(userInfo).then(res => {
        const { data } = res
        if (data) {
          // 登录成功，存储用户信息
          commit('SET_USER', data)
          resolve()
        } else {
          // 登录失败
          reject(res)
        }
      }).catch(error => {
        reject(error)
      })
    })
  },

  // 获取用户信息
  getInfo({ commit, state }) {
    return new Promise((resolve, reject) => {
      getInfo(state.token).then(response => {
        const { data } = response
        if (!data) {
          return reject('Verification failed, please Login again.')
        }
        commit('SET_NAME', data.name)
        commit('SET_AVATAR', data.avatar)
        resolve(data)
      }).catch(error => {
        reject(error)
      })
    })
  },

  // 用户登出
  logout({ commit, state }) {
    return new Promise((resolve, reject) => {
      logout(state.token).then(() => {
        removeToken()
        resetRouter()
        commit('RESET_STATE')
        resolve()
      }).catch(error => {
        reject(error)
      })
    })
  },

  // 移除 token
  resetToken({ commit }) {
    return new Promise(resolve => {
      removeToken()
      commit('RESET_STATE')
      resolve()
    })
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
```

### 2. Getters 配置

`src/store/getters.js`：

```javascript
const getters = {
  user: state => state.user.user
}
export default getters
```

## 登录功能实现

### 1. 登录方法实现

```javascript
methods: {
  handleLogin() {
    this.$refs.loginForm.validate((valid) => {
      if (valid) {
        // 表单验证通过
        this.loading = true

        // 如果选择了7天内免登录
        if (this.loginForm.checked) {
          this.loginForm.remember = 7
        }

        // 调用 Vuex 登录 action
        this.$store
          .dispatch("user/login", this.loginForm)
          .then(() => {
            // 登录成功，跳转到首页或重定向页面
            this.$router.push({ path: this.redirect || "/" })
            this.loading = false
          })
          .catch((res) => {
            // 登录失败，显示错误信息
            if (typeof res === 'string') {
              // 验证码错误
              this.$message.error('验证码错误')
            } else {
              // 账号密码错误
              this.$message.error('账号密码错误')
            }
            // 重新获取验证码
            this.getCaptchaFunc()
            this.loading = false
            this.loginForm.captcha = ''
          })
      } else {
        // 表单验证失败
        console.log("error submit!!")
        return false
      }
    })
  }
}
```

### 2. 错误处理机制

```javascript
.catch((res) => {
  // 根据错误类型显示不同的提示
  if (typeof res === 'string') {
    // 字符串类型错误 - 验证码错误
    this.$message.error('验证码错误')
  } else {
    // 对象类型错误 - 账号密码错误
    this.$message.error('账号密码错误')
  }
  
  // 重新获取验证码
  this.getCaptchaFunc()
  this.loading = false
  this.loginForm.captcha = ''  // 清空验证码输入
})
```

## 认证工具函数

### 1. Token 管理

`src/utils/auth.js`：

```javascript
const TokenKey = 'vue_admin_template_token'

export function getToken() {
  return localStorage.getItem(TokenKey)
}

export function setToken(token) {
  return localStorage.setItem(TokenKey, token)
}

export function removeToken() {
  return localStorage.removeItem(TokenKey)
}
```

### 2. 验证函数

`src/utils/validate.js`：

```javascript
/**
 * 验证用户名
 * @param {string} str
 * @returns {Boolean}
 */
export function validUsername(str) {
  const valid_map = ['admin', 'editor']
  return valid_map.indexOf(str.trim()) >= 0
}

/**
 * 验证 URL
 * @param {string} url
 * @returns {Boolean}
 */
export function validURL(url) {
  const reg = /^(https?|ftp):\/\/([a-zA-Z0-9.-]+(:[a-zA-Z0-9.&%$-]+)*@)*((25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9][0-9]?)(\.(25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9]?[0-9])){3}|([a-zA-Z0-9-]+\.)*[a-zA-Z0-9-]+\.(com|edu|gov|int|mil|net|org|biz|arpa|info|name|pro|aero|coop|museum|[a-zA-Z]{2}))(:[0-9]+)*(\/($|[a-zA-Z0-9.,?'\\+&%$#=~_-]+))*$/
  return reg.test(url)
}
```

## 登录状态保持

### 1. 7天内免登录

```javascript
// 如果用户选择了7天内免登录
if (this.loginForm.checked) {
  this.loginForm.remember = 7  // 告诉服务器记住7天
}
```

### 2. Token 持久化

```javascript
// 在响应拦截器中自动存储 token
if (response.headers.authentication) {
  localStorage.adminToken = response.headers.authentication;
}
```

## 路由跳转

### 1. 重定向处理

```javascript
watch: {
  $route: {
    handler: function(route) {
      // 获取重定向路径
      this.redirect = route.query && route.query.redirect
    },
    immediate: true
  }
}
```

### 2. 登录成功跳转

```javascript
.then(() => {
  // 跳转到重定向页面或首页
  this.$router.push({ path: this.redirect || "/" })
  this.loading = false
})
```

## 完整登录流程

### 1. 用户操作流程

```
1. 用户输入账号、密码、验证码
2. 选择是否7天内免登录
3. 点击登录按钮
4. 触发表单验证
```

### 2. 系统处理流程

```
5. 验证表单数据
6. 添加 remember 参数（如果选择免登录）
7. 显示加载状态
8. 调用登录接口
```

### 3. 服务器处理

```
9. 接收登录数据
10. 验证验证码
11. 验证账号密码
12. 生成 Token
13. 返回用户信息和 Token
```

### 4. 客户端响应

```
14. 接收响应数据
15. 提取 Token 并存储
16. 存储用户信息到 Vuex
17. 跳转到目标页面
18. 隐藏加载状态
```

### 5. 错误处理

```
19. 捕获错误信息
20. 判断错误类型
21. 显示错误提示
22. 刷新验证码
23. 清空验证码输入
24. 隐藏加载状态
```

## 测试要点

### 1. 正常登录测试

- 输入正确的账号密码
- 输入正确的验证码
- 检查是否成功跳转

### 2. 错误处理测试

- 验证码错误测试
- 账号密码错误测试
- 网络错误测试

### 3. 免登录测试

- 选择7天内免登录
- 检查 Token 是否正确存储
- 刷新页面检查登录状态

### 4. 表单验证测试

- 必填项验证
- 实时验证反馈
- 错误提示显示

## 常见问题

### 1. Token 未保存

- 检查响应拦截器是否正确配置
- 检查 localStorage 是否可用
- 检查响应头是否包含 authentication

### 2. 登录后未跳转

- 检查路由配置
- 检查重定向逻辑
- 检查登录成功回调

### 3. 验证码不刷新

- 检查获取验证码方法
- 检查错误处理逻辑
- 检查验证码清空逻辑

## 优化建议

### 1. 用户体验

- 添加登录加载动画
- 优化错误提示样式
- 添加登录成功反馈

### 2. 安全性

- 添加密码加密
- 实现 Token 刷新机制
- 添加登录次数限制

### 3. 功能完善

- 添加忘记密码功能
- 添加记住账号功能
- 添加第三方登录

## 下一步

登录功能完成后，接下来我们将：

1. 实现鉴权功能
2. 添加路由守卫
3. 实现权限控制
4. 完善用户信息管理

## 总结

本节课程我们完整实现了登录功能，包括：

1. API 接口封装
2. 请求和响应拦截器配置
3. Vuex 状态管理
4. 登录流程实现
5. 错误处理机制
6. Token 管理
7. 路由跳转功能

这为我们的博客管理系统提供了完整的用户认证功能，为后续的权限控制打下了基础。
