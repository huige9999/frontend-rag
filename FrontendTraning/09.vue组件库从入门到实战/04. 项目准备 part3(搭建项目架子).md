# 04. 项目准备 part3 - 搭建项目架子

## 课程概述

本节课程将基于 vue-admin-template 模板搭建我们的博客管理系统项目架子，包括项目初始化、目录结构介绍、核心文件说明等。

## 项目初始化

### 1. 项目来源

基于 vue-admin-template 模板进行二次开发，该模板已经为我们配置好了：

- Vue 2.6.10
- Vue Router 3.0.6
- Vuex 3.1.0
- Element UI 2.13.2
- Axios 0.18.1
- 完整的构建配置

### 2. 项目结构

```
background-system/
├── build/                  # 构建相关配置
│   └── index.js           # 构建脚本
├── public/                 # 静态资源
│   ├── favicon.ico
│   └── index.html
├── src/                    # 源代码目录
│   ├── api/               # API 接口封装
│   │   ├── table.js       # 表格相关接口
│   │   └── user.js        # 用户相关接口
│   ├── components/        # 公共组件
│   │   ├── Breadcrumb/    # 面包屑组件
│   │   ├── Hamburger/     # 汉堡菜单组件
│   │   └── SvgIcon/       # SVG图标组件
│   ├── icons/             # SVG图标
│   ├── layout/            # 布局组件
│   │   ├── components/    # 布局子组件
│   │   │   ├── AppMain.vue    # 主内容区域
│   │   │   ├── Navbar.vue     # 顶部导航栏
│   │   │   └── Sidebar/       # 侧边栏
│   │   │       ├── index.vue
│   │   │       ├── Item.vue
│   │   │       ├── Link.vue
│   │   │       ├── Logo.vue
│   │   │       └── SidebarItem.vue
│   │   ├── index.vue     # 布局主组件
│   │   └── mixin/        # 混入
│   ├── router/            # 路由配置
│   │   └── index.js      # 路由主文件
│   ├── store/             # Vuex 状态管理
│   │   ├── getters.js    # getters
│   │   ├── index.js      # store 主文件
│   │   └── modules/      # store 模块
│   │       ├── app.js     # 应用状态
│   │       ├── settings.js # 设置状态
│   │       └── user.js    # 用户状态
│   ├── utils/             # 工具函数
│   │   ├── auth.js       # 认证相关
│   │   ├── get-page-title.js
│   │   ├── index.js      # 通用工具
│   │   ├── request.js    # 请求封装
│   │   └── validate.js   # 验证函数
│   ├── views/             # 页面组件
│   │   ├── 404.vue       # 404页面
│   │   ├── dashboard/    # 控制台
│   │   ├── form/         # 表单示例
│   │   ├── login/        # 登录页面
│   │   ├── nested/       # 嵌套路由示例
│   │   ├── table/        # 表格示例
│   │   └── tree/         # 树形控件示例
│   ├── App.vue           # 根组件
│   ├── main.js           # 入口文件
│   ├── permission.js     # 权限控制
│   └── settings.js       # 全局设置
├── tests/                 # 测试文件
├── .eslintrc.js           # ESLint配置
├── babel.config.js        # Babel配置
├── jest.config.js         # Jest配置
├── package.json           # 项目依赖
├── postcss.config.js      # PostCSS配置
└── vue.config.js          # Vue CLI配置
```

## 核心文件说明

### 1. main.js - 应用入口

```javascript
import Vue from 'vue'
import ElementUI from 'element-ui'
import 'element-ui/lib/theme-chalk/index.css'
import App from './App'
import router from './router'
import store from './store'
import './icons' // icon
import './permission' // 权限控制

Vue.use(ElementUI)

new Vue({
  el: '#app',
  router,
  store,
  render: h => h(App)
})
```

### 2. router/index.js - 路由配置

```javascript
export const constantRoutes = [
  {
    path: '/login',
    component: () => import('@/views/login/index'),
    hidden: true
  },
  {
    path: '/404',
    component: () => import('@/views/404'),
    hidden: true
  },
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [{
      path: 'dashboard',
      name: 'Dashboard',
      component: () => import('@/views/dashboard/index'),
      meta: { title: 'Dashboard', icon: 'dashboard' }
    }]
  }
]
```

### 3. store/index.js - 状态管理

```javascript
import Vue from 'vue'
import Vuex from 'vuex'
import getters from './getters'
import app from './modules/app'
import settings from './modules/settings'
import user from './modules/user'

Vue.use(Vuex)

const store = new Vuex.Store({
  modules: {
    app,
    settings,
    user
  },
  getters
})

export default store
```

### 4. utils/request.js - 请求封装

```javascript
import axios from 'axios'
import { Message } from 'element-ui'

// 创建axios实例
const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API,
  timeout: 5000
})

// 响应拦截器
service.interceptors.response.use(
  response => {
    const res = response.data
    return res
  },
  error => {
    Message({
      message: error.message,
      type: 'error',
      duration: 5 * 1000
    })
    return Promise.reject(error)
  }
)

export default service
```

## 项目启动

### 安装依赖

```bash
npm install
```

### 开发环境运行

```bash
npm run dev
```

### 生产环境构建

```bash
npm run build:prod
```

## 核心依赖说明

### 生产依赖

- **vue (2.6.10)** - Vue.js 核心框架
- **vue-router (3.0.6)** - 官方路由管理器
- **vuex (3.1.0)** - 状态管理模式
- **element-ui (2.13.2)** - 基于 Vue 的 UI 组件库
- **axios (0.18.1)** - HTTP 请求库
- **js-cookie (2.2.0)** - Cookie 操作
- **nprogress (0.2.0)** - 进度条组件

### 开发依赖

- **@vue/cli-service (4.4.4)** - Vue CLI 服务
- **eslint (6.7.2)** - 代码检查工具
- **sass (1.26.8)** - CSS 预处理器
- **mockjs (1.0.1-beta3)** - 数据模拟

## 项目特点

### 1. 模块化设计

- API 接口模块化
- 组件化开发
- Vuex 模块化状态管理
- 路由模块化配置

### 2. 工程化配置

- ESLint 代码规范
- Babel 转译
- 环境变量配置
- 热重载开发服务器

### 3. 最佳实践

- 目录结构清晰
- 代码分离
- 组件复用
- 权限控制

## 下一步

项目架子已经搭建完成，接下来我们将：

1. 配置服务器和接口
2. 制作登录界面
3. 实现登录功能
4. 添加鉴权功能

## 总结

本节课程我们基于 vue-admin-template 成功搭建了项目架子，了解了项目的整体结构和核心文件的作用。这个项目架子为我们后续的开发提供了坚实的基础，包含了完整的前端工程化配置和最佳实践。
