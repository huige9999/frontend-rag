# 第11章：文章模块 part3（添加和编辑文章）

## 本章概述

本章将学习文章的添加和编辑功能，重点掌握 Toast UI 富文本编辑器的使用，实现文章内容的富文本编辑和发布。

## 学习目标

- 掌握 Toast UI 富文本编辑器的使用
- 学会处理 Markdown 和 HTML 内容
- 理解富文本编辑器的数据获取
- 实现文章添加功能
- 实现文章编辑功能

## 核心内容

### 1. 安装 Toast UI 编辑器

```bash
npm install @toast-ui/vue-editor
```

### 2. 创建添加文章页面

创建 `src/views/addBlog/index.vue`：

```vue
<template>
  <div class="app-container">
    <!-- 文章标题 -->
    <div class="block">文章标题</div>
    <div style="margin-bottom: 15px">
      <el-input v-model="form.title" placeholder="请输入文章标题"></el-input>
    </div>

    <!-- 文章编辑 -->
    <div class="block">文章编辑</div>
    <editor
      ref="toastuiEditor"
      :initialValue="form.editorText"
      height="600px"
    />

    <!-- 文章描述 -->
    <div class="block">文章描述</div>
    <el-input type="textarea" v-model="form.description" :rows="6"></el-input>

    <!-- 文章头图 -->
    <Upload uploadTitle="文章头图" v-model="form.thumb" />

    <!-- 选择分类 -->
    <div class="block">选择分类</div>
    <el-select
      v-model="form.select"
      slot="prepend"
      placeholder="请选择文章分类"
    >
      <el-option
        v-for="item in blogType"
        :key="item.id"
        :label="item.name"
        :value="item.id"
      ></el-option>
    </el-select>

    <div>
      <!-- 发布按钮 -->
      <el-button
        type="primary"
        style="margin-top: 15px"
        @click="addArticleHandle"
      >发布文章</el-button>
    </div>
  </div>
</template>

<script>
import "@toast-ui/editor/dist/toastui-editor.css";
import { Editor } from "@toast-ui/vue-editor";
import Upload from "@/components/Upload.vue";
import { getBlogType } from "@/api/blogType.js";
import { addBlog } from '@/api/blog.js'

export default {
  data() {
    return {
      form: {
        title: "", // 文章标题
        editorText: "", // 用户编辑的内容
        description: "", // 文章的描述
        thumb: "", // 文章的图片
        select: "", // 选择分类
      },
      blogType: [], // 存放博客分类
      imageUrl: "", // 图片在服务器上面的地址
    };
  },
  created() {
    // 一进来的时候，就需要拿取分类的数据
    getBlogType().then(({ data }) => {
      this.blogType = data;
    });
  },
  components: {
    editor: Editor,
    Upload,
  },
  methods: {
    addArticleHandle() {
      // 1. 获取富文本编辑器的 HTML 和 Markdown 内容
      let html = this.$refs.toastuiEditor.invoke("getHTML");
      let markdown = this.$refs.toastuiEditor.invoke("getMarkdown");

      // 2. 组装要传递给服务器的对象
      let obj = {
        title: this.form.title,
        description: this.form.description,
        createDate: new Date().getTime(),
        categoryId: this.form.select,
        // toc 传递一个空数组过去，之后在服务器根据 markdown 的内容来生成 toc 目录
        toc: [],
        htmlContent: html,
        thumb: this.form.thumb,
        markdownContent: markdown
      };

      // 3. 验证并提交数据
      if(obj.title && obj.description && obj.htmlContent && obj.categoryId){
        addBlog(obj).then(()=>{
          this.$router.push('/blogList'); // 跳转到文章列表
          this.$message.success('文章发布成功');
        })
      } else {
        this.$message.error('请填写所有内容');
      }
    },
  },
};
</script>

<style lang="scss" scoped>
.block {
  margin: 15px 0;
  font-weight: 100;
}
</style>
```

### 3. 创建编辑文章页面

创建 `src/views/editBlog/index.vue`：

```vue
<template>
  <div class="app-container">
    <!-- 文章标题 -->
    <div class="block">文章标题</div>
    <div style="margin-bottom: 15px">
      <el-input v-model="form.title" placeholder="请输入文章标题"></el-input>
    </div>

    <!-- 文章编辑 -->
    <div class="block">文章编辑</div>
    <editor
      ref="toastuiEditor"
      :initialValue="form.editorText"
      height="600px"
    />

    <!-- 文章描述 -->
    <div class="block">文章描述</div>
    <el-input type="textarea" v-model="form.description" :rows="6"></el-input>

    <!-- 文章头图 -->
    <Upload uploadTitle="文章头图" v-model="form.thumb" />

    <!-- 选择分类 -->
    <div class="block">选择分类</div>
    <el-select
      v-model="form.select"
      slot="prepend"
      placeholder="请选择文章分类"
      @change="changeHandle"
    >
      <el-option
        v-for="item in blogType"
        :key="item.id"
        :label="item.name"
        :value="item.id"
      ></el-option>
    </el-select>

    <div>
      <!-- 确认修改按钮 -->
      <el-button
        type="primary"
        style="margin-top: 15px"
        @click="editArticleHandle"
      >确认修改</el-button>
    </div>
  </div>
</template>

<script>
import "@toast-ui/editor/dist/toastui-editor.css";
import { Editor } from "@toast-ui/vue-editor";
import Upload from "@/components/Upload.vue";
import { getBlogType } from "@/api/blogType.js";
import { editBlog, findOneBlog } from '@/api/blog.js'

export default {
  data() {
    return {
      id: null, // 存储传递过来的 id
      form: {
        title: "", // 文章标题
        editorText: "", // 用户编辑的内容
        description: "", // 文章的描述
        thumb: "", // 文章的图片
        select: "", // 选择分类
      },
      blogType: [], // 存放博客分类
      imageUrl: "", // 图片在服务器上面的地址
    };
  },
  created() {
    // 1. 获取分类数据
    getBlogType().then(({ data }) => {
      this.blogType = data;
    });

    // 2. 获取文章详情并回填
    this.id = this.$route.params.id;
    findOneBlog(this.id).then(({data})=>{
      // 将内容回填至表单
      this.form = data;
      this.form.select = data.category === null ? '' : data.category.id; 
      this.$refs.toastuiEditor.invoke('setHTML', data.htmlContent);
    })
  },
  components: {
    editor: Editor,
    Upload,
  },
  methods: {
    editArticleHandle() {
      // 1. 获取富文本编辑器的 HTML 和 Markdown 内容
      let html = this.$refs.toastuiEditor.invoke("getHTML");
      let markdown = this.$refs.toastuiEditor.invoke("getMarkdown");

      // 2. 组装要传递给服务器的对象
      let obj = {
        title: this.form.title,
        description: this.form.description,
        createDate: new Date().getTime(),
        categoryId: this.form.select,
        toc: [],
        htmlContent: html,
        thumb: this.form.thumb,
        markdownContent: markdown
      };

      // 3. 验证并提交数据
      if(obj.title && obj.description && obj.htmlContent && obj.categoryId){
        editBlog({id: this.form.id, data: obj}).then(()=>{
          this.$router.push('/blogList'); // 跳转到文章列表
          this.$message.success('文章修改成功');
        })
      } else {
        this.$message.error('请填写所有内容');
      }
    },
    
    // 选择器改变事件
    changeHandle(){
      this.$forceUpdate();
    }
  },
};
</script>

<style lang="scss" scoped>
.block {
  margin: 15px 0;
  font-weight: 100;
}
</style>
```

### 4. 更新文章 API

在 `src/api/blog.js` 中添加接口：

```javascript
import request from '@/utils/request'

// 分页获取文章
export function findBlog(page=1, limit=10){
  return request({
    url : '/api/blog',
    method : 'get',
    params : {
      page,
      limit
    }
  })
}

// 删除文章
export function delOneBlog(id){
  return request({
    url : `/api/blog/${id}`,
    method : 'delete'
  })
}

// 添加文章
export function addBlog(data){
  return request({
    url : '/api/blog',
    method : 'post',
    data
  })
}

// 编辑文章
// 调用这个接口时，需要传递一个对象
// {
//     id : 要修改的文章的 id,
//     data : 修改的文章的所有内容
// }
export function editBlog(blogInfo){
  return request({
    url : `/api/blog/${blogInfo.id}`,
    method : 'put',
    data : blogInfo.data
  })
}

// 根据 id 查找某一篇文章
export function findOneBlog(id){
  return request({
    url : `/api/blog/${id}`,
    method : 'get'
  })
}
```

## 技术要点解析

### 1. Toast UI 编辑器

**基本使用**：

```vue
<template>
  <editor
    ref="toastuiEditor"
    :initialValue="form.editorText"
    height="600px"
  />
</template>

<script>
import { Editor } from "@toast-ui/vue-editor";

export default {
  components: {
    editor: Editor,
  },
  methods: {
    // 获取 HTML 内容
    getHTML() {
      return this.$refs.toastuiEditor.invoke("getHTML");
    },
    
    // 获取 Markdown 内容
    getMarkdown() {
      return this.$refs.toastuiEditor.invoke("getMarkdown");
    },
    
    // 设置 HTML 内容
    setHTML(html) {
      this.$refs.toastuiEditor.invoke("setHTML", html);
    }
  }
}
</script>
```

### 2. 编辑器常用方法

```javascript
// 获取 HTML 内容
this.$refs.toastuiEditor.invoke("getHTML");

// 获取 Markdown 内容
this.$refs.toastuiEditor.invoke("getMarkdown");

// 设置 HTML 内容
this.$refs.toastuiEditor.invoke("setHTML", htmlContent);

// 设置 Markdown 内容
this.$refs.toastuiEditor.invoke("setMarkdown", markdownContent);

// 获取纯文本内容
this.$refs.toastuiEditor.invoke("getText");
```

### 3. 路由参数获取

```javascript
// 获取路由中的参数
this.id = this.$route.params.id;

// 路由配置
{
  path: '/editBlog/:id',
  name: 'EditBlog',
  component: () => import('@/views/editBlog/index')
}
```

### 4. 数据回填技术

```javascript
// 获取文章详情
findOneBlog(this.id).then(({data})=>{
  // 直接回填基本数据
  this.form = data;
  
  // 处理特殊情况
  this.form.select = data.category === null ? '' : data.category.id; 
  
  // 设置编辑器内容
  this.$refs.toastuiEditor.invoke('setHTML', data.htmlContent);
})
```

### 5. 表单数据组装

```javascript
let obj = {
  title: this.form.title,
  description: this.form.description,
  createDate: new Date().getTime(),
  categoryId: this.form.select,
  toc: [],
  htmlContent: html,
  thumb: this.form.thumb,
  markdownContent: markdown
};
```

### 6. 条件验证

```javascript
// 验证所有必填项
if(obj.title && obj.description && obj.htmlContent && obj.categoryId){
  // 验证通过，提交数据
  addBlog(obj).then(()=>{
    this.$router.push('/blogList');
  })
} else {
  // 验证失败，提示错误
  this.$message.error('请填写所有内容');
}
```

## Toast UI 编辑器高级配置

### 1. 设置语言

```javascript
import "@toast-ui/editor/dist/i18n/zh-cn";

export default {
  data() {
    return {
      editorOptions: {
        language: "zh-CN",
      }
    };
  }
}
```

### 2. 自定义工具栏

```javascript
editorOptions: {
  toolbarItems: [
    ['heading', 'bold', 'italic', 'strike'],
    ['hr', 'quote'],
    ['ul', 'ol', 'task'],
    ['table', 'image', 'link'],
    ['code', 'codeblock']
  ]
}
```

### 3. 设置编辑器模式

```javascript
editorOptions: {
  // WYSIWYG 模式
  initialEditType: 'wysiwyg',
  
  // Markdown 模式
  // initialEditType: 'markdown',
}
```

## 数据结构说明

### 文章数据结构

```javascript
{
  id: "文章ID",
  title: "文章标题",
  description: "文章描述",
  htmlContent: "HTML格式内容",
  markdownContent: "Markdown格式内容",
  thumb: "文章头图路径",
  categoryId: "分类ID",
  createDate: "创建时间",
  toc: [], // 目录
  scanNumber: "浏览数",
  commentNumber: "评论数"
}
```

### 分类数据结构

```javascript
{
  id: "分类ID",
  name: "分类名称",
  order: "排序等级",
  articleCount: "文章数量"
}
```

## 路由配置

```javascript
{
  path: '/addBlog',
  name: 'AddBlog',
  component: () => import('@/views/addBlog/index'),
  meta: { title: '添加文章' }
},
{
  path: '/editBlog/:id',
  name: 'EditBlog',
  component: () => import('@/views/editBlog/index'),
  meta: { title: '编辑文章' }
}
```

## 本章小结

本章实现了文章的添加和编辑功能：

1. 掌握了 Toast UI 富文本编辑器的使用
2. 学会了获取和设置编辑器内容
3. 实现了文章添加功能
4. 实现了文章编辑和数据回填
5. 理解了路由参数的获取

## 课后练习

1. 实现文章草稿保存功能
2. 添加文章定时发布功能
3. 实现文章版本管理
4. 优化图片上传和插入
5. 添加代码高亮功能

## 下一章预告

下一章将学习如何优化文章添加和编辑功能，通过组件复用来简化代码。