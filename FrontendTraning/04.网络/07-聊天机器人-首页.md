# 聊天机器人 - 首页

## 章节概述

本章节我们将完成聊天机器人项目的首页功能，这是整个项目的核心交互界面。首页主要包括聊天消息显示区域、消息发送功能、用户信息展示以及历史记录加载等功能。

## 学习目标

- 掌握聊天界面的布局与样式设计
- 学会实现消息的发送与接收逻辑
- 理解历史记录的加载与显示
- 掌握用户身份验证与页面跳转

## 项目结构

```
04. 网络/12. 聊天机器人-首页/
├── index.html          # 首页HTML结构
├── login.html          # 登录页面
├── reg.html            # 注册页面
├── css/
│   ├── common.css      # 公共样式
│   ├── index.css       # 首页样式
│   └── user.css        # 用户相关页面样式
└── js/
    ├── common.js       # 公共工具函数
    ├── api.js          # API接口封装
    ├── index.js        # 首页逻辑
    ├── login.js        # 登录逻辑
    └── reg.js          # 注册逻辑
```

## 核心知识点

### 1. 首页布局结构

首页采用左右分栏布局，左侧为聊天主区域，右侧为用户信息区域。

```html
<div class="container">
  <div class="main">
    <h1 class="main-title">
      <img src="./asset/robot.png" alt="" />
      AI 聊天机器人
    </h1>
    <div class="chat-container"></div>
    <form class="msg-container">
      <input
        type="text"
        id="txtMsg"
        placeholder="在这里输入要发送的消息，按回车键即可发送"
      />
      <button>发送</button>
    </form>
  </div>
  <div class="aside">
    <img src="./asset/avatar.png" class="aside-avatar" alt="" />
    <p id="nickname" class="aside-name"></p>
    <p id="loginId" class="aside-account"></p>
  </div>
  <div class="close">
    <i class="iconfont icon-close"></i>
  </div>
</div>
```

**布局要点：**
- 使用flexbox实现左右分栏
- 主区域包含标题、聊天容器、消息输入框
- 侧边栏显示用户头像和账号信息
- 右上角关闭按钮用于退出登录

### 2. 样式设计

#### 容器样式

```css
.container {
  width: 870px;
  height: 600px;
  background: #fff;
  display: flex;
  position: relative;
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.2);
  color: #333;
}

.main {
  width: 600px;
  flex: 0 0 auto;
}

.aside {
  flex: 1 0 auto;
  padding-top: 80px;
  border-left: 1px solid #eee;
}
```

#### 聊天消息样式

```css
.chat-item {
  padding-bottom: 25px;
  display: flex;
  position: relative;
  width: 100%;
  margin-bottom: 10px;
}

.chat-content {
  font-size: 14px;
  border: 1px solid #d8d8d8;
  padding: 8px;
  background-color: #fff;
  border-radius: 6px;
  position: relative;
  line-height: 1.5;
  max-width: 450px;
}

/* 用户自己的消息 */
.chat-item.me {
  flex-direction: row-reverse;
}

.chat-item.me .chat-content {
  background: #98e855;
  border-color: #88cf4d;
}
```

**样式要点：**
- 使用flex布局实现消息左右对齐
- 通过`flex-direction: row-reverse`实现用户消息右对齐
- 使用伪元素创建聊天气泡效果
- 自定义滚动条样式提升用户体验

### 3. API接口封装

```javascript
var API = (function () {
  const BASE_URL = 'https://study.duyiedu.com';
  const TOKEN_KEY = 'token';

  // GET请求封装
  function get(path) {
    const headers = {};
    const token = localStorage.getItem(TOKEN_KEY);
    if (token) {
      headers.authorization = `Bearer ${token}`;
    }
    return fetch(BASE_URL + path, { headers });
  }

  // POST请求封装
  function post(path, bodyObj) {
    const headers = {
      'Content-Type': 'application/json',
    };
    const token = localStorage.getItem(TOKEN_KEY);
    if (token) {
      headers.authorization = `Bearer ${token}`;
    }
    return fetch(BASE_URL + path, {
      headers,
      method: 'POST',
      body: JSON.stringify(bodyObj),
    });
  }

  // 发送聊天消息
  async function sendChat(content) {
    const resp = await post('/api/chat', {
      content,
    });
    return await resp.json();
  }

  // 获取聊天历史记录
  async function getHistory() {
    const resp = await get('/api/chat/history');
    return await resp.json();
  }

  // 获取用户信息
  async function profile() {
    const resp = await get('/api/user/profile');
    return await resp.json();
  }

  // 退出登录
  function loginOut() {
    localStorage.removeItem(TOKEN_KEY);
  }

  return {
    reg,
    login,
    exists,
    profile,
    sendChat,
    getHistory,
    loginOut,
  };
})();
```

**API封装要点：**
- 使用模块模式封装API，避免全局污染
- 统一处理token认证逻辑
- 使用async/await处理异步请求
- 返回公共接口供外部调用

### 4. 首页逻辑实现

#### 用户身份验证

```javascript
(async function () {
  // 验证是否有登录，如果没有登录，跳转到登录页
  const resp = await API.profile();
  const user = resp.data;
  if (!user) {
    alert('未登录或登录已过期，请重新登录');
    location.href = './login.html';
    return;
  }

  // 下面的代码环境，一定是登录状态
  setUserInfo();

  // 设置用户信息
  function setUserInfo() {
    doms.aside.nickname.innerText = user.nickname;
    doms.aside.loginId.innerText = user.loginId;
  }
})();
```

#### 加载历史记录

```javascript
// 加载历史记录
await loadHistory();

async function loadHistory() {
  const resp = await API.getHistory();
  for (const item of resp.data) {
    addChat(item);
  }
  scrollBottom();
}

// 让聊天区域的滚动条滚动到底
function scrollBottom() {
  doms.chatContainer.scrollTop = doms.chatContainer.scrollHeight;
}
```

#### 添加聊天消息

```javascript
/*
消息对象结构：
{
  content: "你几岁啦？"
  createdAt: 1651213093992
  from: "haha"
  to: null
}
*/

function addChat(chatInfo) {
  const div = $$$('div');
  div.classList.add('chat-item');
  
  // 判断是否为用户发送的消息
  if (chatInfo.from) {
    div.classList.add('me');
  }
  
  const img = $$$('img');
  img.className = 'chat-avatar';
  img.src = chatInfo.from ? './asset/avatar.png' : './asset/robot-avatar.jpg';

  const content = $$$('div');
  content.className = 'chat-content';
  content.innerText = chatInfo.content;

  const date = $$$('div');
  date.className = 'chat-date';
  date.innerText = formatDate(chatInfo.createdAt);

  div.appendChild(img);
  div.appendChild(content);
  div.appendChild(date);

  doms.chatContainer.appendChild(div);
}

// 时间格式化函数
function formatDate(timestamp) {
  const date = new Date(timestamp);
  const year = date.getFullYear();
  const month = (date.getMonth() + 1).toString().padStart(2, '0');
  const day = date.getDate().toString().padStart(2, '0');
  const hour = date.getHours().toString().padStart(2, '0');
  const minute = date.getMinutes().toString().padStart(2, '0');
  const second = date.getSeconds().toString().padStart(2, '0');

  return `${year}-${month}-${day} ${hour}:${minute}:${second}`;
}
```

#### 发送消息功能

```javascript
// 发送消息事件
doms.msgContainer.onsubmit = function (e) {
  e.preventDefault();
  sendChat();
};

async function sendChat() {
  const content = doms.txtMsg.value.trim();
  if (!content) {
    return;
  }
  
  // 立即显示用户消息
  addChat({
    from: user.loginId,
    to: null,
    createdAt: Date.now(),
    content,
  });
  
  doms.txtMsg.value = '';
  scrollBottom();
  
  // 发送到服务器
  const resp = await API.sendChat(content);
  
  // 显示机器人回复
  addChat({
    from: null,
    to: user.loginId,
    ...resp.data,
  });
  
  scrollBottom();
}
```

**发送消息要点：**
- 立即显示用户消息，提升用户体验
- 异步发送请求，等待服务器响应
- 收到响应后显示机器人回复
- 每次添加消息后滚动到底部

#### 退出登录

```javascript
// 退出登录事件
doms.close.onclick = function () {
  API.loginOut();
  location.href = './login.html';
};
```

### 5. 公共工具函数

```javascript
// 选择器工具函数
function $(selector) {
  return document.querySelector(selector);
}

function $$(selector) {
  return document.querySelectorAll(selector);
}

function $$$(tagName) {
  return document.createElement(tagName);
}
```

## 关键技术点

### 1. Token认证机制

```javascript
// 在请求头中添加token
const token = localStorage.getItem(TOKEN_KEY);
if (token) {
  headers.authorization = `Bearer ${token}`;
}
```

- 使用localStorage存储token
- 每次请求在请求头中携带token
- 服务器通过token验证用户身份

### 2. 消息区分显示

通过`from`字段判断消息发送者：
- `from`有值：用户发送的消息，右对齐显示
- `from`为null：机器人发送的消息，左对齐显示

### 3. 滚动条控制

```javascript
function scrollBottom() {
  doms.chatContainer.scrollTop = doms.chatContainer.scrollHeight;
}
```

- 设置scrollTop为scrollHeight实现滚动到底部
- 配合CSS的`scroll-behavior: smooth`实现平滑滚动

### 4. 表单提交处理

```javascript
doms.msgContainer.onsubmit = function (e) {
  e.preventDefault(); // 阻止表单默认提交行为
  sendChat();          // 执行发送消息逻辑
};
```

## 页面交互流程

1. **页面加载**
   - 验证用户登录状态
   - 加载用户信息
   - 加载历史聊天记录

2. **发送消息**
   - 用户输入消息内容
   - 按回车或点击发送按钮
   - 立即显示用户消息
   - 异步发送到服务器
   - 显示机器人回复

3. **退出登录**
   - 点击关闭按钮
   - 清除本地token
   - 跳转到登录页面

## 用户体验优化

### 1. 即时反馈
- 用户发送消息后立即显示，不等服务器响应
- 添加平滑滚动效果

### 2. 输入优化
- 支持回车键发送消息
- 发送后自动清空输入框
- 输入框自动聚焦

### 3. 视觉反馈
- 用户消息和机器人消息使用不同样式
- 消息气泡指向性设计
- 时间戳显示

### 4. 错误处理
- 未登录自动跳转
- 空消息不发送
- 网络错误提示

## 常见问题

### 1. 如何判断消息是用户发送还是机器人回复？

通过消息对象的`from`字段：
```javascript
if (chatInfo.from) {
  // 用户消息，添加'me'类
  div.classList.add('me');
}
```

### 2. 如何实现消息自动滚动到底部？

设置scrollTop为scrollHeight：
```javascript
doms.chatContainer.scrollTop = doms.chatContainer.scrollHeight;
```

### 3. 如何处理用户未登录的情况？

在页面加载时验证用户状态：
```javascript
const resp = await API.profile();
const user = resp.data;
if (!user) {
  alert('未登录或登录已过期，请重新登录');
  location.href = './login.html';
  return;
}
```

## 扩展功能建议

1. **消息持久化**：将消息保存到本地，提升加载速度
2. **消息搜索**：添加搜索功能查找历史消息
3. **表情支持**：集成表情包功能
4. **文件上传**：支持发送图片和文件
5. **消息撤回**：实现消息撤回功能
6. **在线状态**：显示用户在线状态
7. **消息已读**：显示消息已读状态
8. **多聊天室**：支持多个聊天频道

## 总结

本章节完成了聊天机器人首页的核心功能，包括：

1. ✅ 聊天界面布局与样式设计
2. ✅ 消息发送与接收逻辑
3. ✅ 历史记录加载与显示
4. ✅ 用户身份验证
5. ✅ 退出登录功能

通过本章节的学习，我们掌握了：
- 复杂的聊天界面布局技巧
- 实时消息交互实现方法
- Token认证的实际应用
- 用户体验优化的各种技巧

下一步可以继续完善其他功能，如消息搜索、表情支持、文件上传等，让聊天机器人功能更加丰富。