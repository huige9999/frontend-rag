# 14. 本地测试组件库

## 概述

组件库打包完成后，在发布到 npm 之前，需要进行本地测试以验证打包结果是否正确。本地测试主要分两种方式：

- **建立软链测试**：适用于测试 ES Module 格式
- **CDN 方式测试**：适用于测试 UMD 格式

## 建立软链测试

### 1. 在组件库项目中创建软链接

首先在 `duyi-plus2` 目录下执行 `npm link`，这会在全局 node_modules 中创建一个指向当前项目的软链接。

### 2. 创建测试项目并链接组件库

创建一个新的 Vue 项目 `test-duyiplus-es`，然后通过 `npm link duyi-plus2` 将组件库链接到测试项目中。

测试项目的 `package.json`：

```json
{
  "name": "test-duyiplus-es",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "vite": "^4.4.11"
  }
}
```

注意：测试项目的依赖中没有显式列出 `duyi-plus2`，因为它是通过 `npm link` 软链接进来的。

### 3. 在测试项目中使用组件库

**main.js**：

```js
import "./assets/main.css";

import duyiplus2 from "duyi-plus2";
import "duyi-plus2/style.css";

import { createApp } from "vue";
import App from "./App.vue";

const app = createApp(App);

app.use(duyiplus2);

app.mount("#app");
```

**App.vue**：

```vue
<template>
  <div>
    <Duyi-Button>默认按钮</Duyi-Button>
    <Duyi-Button type="primary">主要按钮</Duyi-Button>
    <Duyi-Button type="info">信息按钮</Duyi-Button>
    <Duyi-Button type="success">成功按钮</Duyi-Button>
    <Duyi-Button type="warning">警告按钮</Duyi-Button>
    <Duyi-Button type="danger">危险按钮</Duyi-Button>
    <Duyi-Card
      img-src="/web.png"
      summary="前端主流布局系统进阶与实战，轻松解决页面布局难题"
    />
    <Duyi-Icon icon="user-secret" size="xl" beat />
    <Duyi-Icon icon="bars" size="xl" beat-fade />
    <Duyi-Icon icon="shield-halved" size="xl" bounce />
    <Duyi-Icon icon="file" size="xl" fade />
    <Duyi-Icon icon="filter" size="xl" flip />
    <Duyi-Icon icon="gear" size="xl" shake />
    <Duyi-Icon icon="barcode" size="xl" spin />
    <Duyi-Icon icon="folder" size="xl" spin spin-reverse />
    <Duyi-Icon icon="folder-open" size="xl" spin-pulse />
  </div>
</template>

<script setup></script>

<style lang="scss" scoped></style>
```

### 组件库 package.json 的 style.css 导出

上一课的 `package.json` 中 `exports` 字段需要包含 `style.css` 子路径导出，才能在测试项目中通过 `import "duyi-plus2/style.css"` 引入样式：

```json
"exports": {
  ".": {
    "import": "./dist/es/duyi-plus.js",
    "require": "./dist/umd/duyi-plus.umd.cjs"
  },
  "./style.css": {
    "import": "./dist/style.css",
    "require": "./dist/umd/style.css"
  }
}
```

## CDN 方式测试

对于 UMD 格式，可以通过直接在 HTML 文件中引入来测试。创建一个 `test-duyiui-umd` 目录，在其中放置 `index.html`：

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="./umd/style.css" />
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script src="./umd/duyi-plus.umd.cjs"></script>
  </head>
  <body>
    <div id="app">
      {{ message }}
      <Duyi-Button>默认按钮</Duyi-Button>
      <Duyi-Button type="primary">主要按钮</Duyi-Button>
      <Duyi-Button type="info">信息按钮</Duyi-Button>
      <Duyi-Button type="success">成功按钮</Duyi-Button>
      <Duyi-Button type="warning">警告按钮</Duyi-Button>
      <Duyi-Button type="danger">危险按钮</Duyi-Button>
      <Duyi-Card
        img-src="/web.png"
        summary="前端主流布局系统进阶与实战，轻松解决页面布局难题"
      />
    </div>
    <script>
      const { createApp, ref } = Vue;

      const app = createApp({
        setup() {
          const message = ref("Hello vue!");
          return {
            message,
          };
        },
      });

      app.use(DuyiPlus);

      app.mount("#app");
    </script>
  </body>
</html>
```

**要点：**

- 通过 CDN 引入 Vue 3：`https://unpkg.com/vue@3/dist/vue.global.js`
- 引入组件库的 UMD 产物：`./umd/duyi-plus.umd.cjs`
- 引入组件库的样式文件：`./umd/style.css`
- 使用 `app.use(DuyiPlus)` 注册所有组件（`DuyiPlus` 是 UMD 打包时配置的全局变量名）
- 组件以 `<Duyi-xxx>` 的方式直接在模板中使用
