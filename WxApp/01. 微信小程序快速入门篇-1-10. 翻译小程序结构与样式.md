# 翻译小程序结构与样式

本节课搭建翻译小程序的整体项目结构，包括页面骨架、全局配置、页面样式等，为后续的功能实现打好基础。

---

## 一、项目全局配置

### 1. app.js — 小程序入口与全局数据

在 `app.js` 中，通过 `App` 构造器定义了全局共享的数据 `globalData`，包含语言列表和默认当前语言，供所有页面使用。

```javascript
// app.js
App({
  onLaunch() {
  },
  // 全局的数据，所有页面都可以使用这个数据
  globalData: {
    // 所有语言的语言列表
    lanList : [
      {'chs': '英文','lang': 'en',"index": 0},
      {'chs': '中文','lang': 'zh',"index": 1},
      {'chs': '日语','lang': 'jp',"index": 2},
      {'chs': '韩语','lang': 'kor',"index": 3},
      {'chs': '法语','lang': 'fra',"index": 4},
      {'chs': '德语','lang': 'de',"index": 5},
      {'chs': '俄语','lang': 'ru',"index": 6},
      {'chs': '泰语','lang': 'th',"index": 7},
      {'chs': '西班牙语','lang': 'spa',"index": 8},
      {'chs': '阿拉伯语','lang': 'ara',"index": 9},
      {'chs': '意大利语','lang': 'it',"index": 10},
      {'chs': '葡萄牙语','lang': 'pt',"index": 11},
      {'chs': '荷兰语','lang': 'nl', 'index':12}
    ],
    // 当前默认是什么语言，默认设置为英语
    curLan : {'chs': '英文','lang': 'en',"index": 0}
  }
})
```

**要点说明：**
- `lanList` 数组中每一项包含 `chs`（中文名）、`lang`（语言代码）、`index`（索引）三个字段
- `curLan` 保存当前选择的目标语言，默认为英语
- 其他页面通过 `getApp().globalData` 即可访问这些数据

### 2. app.json — 全局页面与导航栏配置

```json
{
  "pages":[
    "pages/index/index",
    "pages/change/change",
    "pages/history/history"
  ],
  "window":{
    "backgroundTextStyle":"light",
    "navigationBarBackgroundColor": "#4b3c96",
    "navigationBarTitleText": "小度翻译",
    "navigationBarTextStyle":"white",
    "backgroundColor": "#4b3c96"
  },
  "tabBar": {
    "borderStyle": "white",
    "position": "bottom",
    "color": "#bfbfbf",
    "selectedColor": "#1c1b21",
    "list": [{
      "pagePath": "pages/index/index",
      "text": "翻译",
      "iconPath": "imgs/icon-1.png",
      "selectedIconPath": "imgs/sel-icon-1.png"
    },{
      "pagePath": "pages/history/history",
      "text": "历史",
      "iconPath": "imgs/icon-2.png",
      "selectedIconPath": "imgs/sel-icon-2.png"
    }]
  },
  "style": "v2",
  "sitemapLocation": "sitemap.json"
}
```

**要点说明：**
- `pages` 数组配置了三个页面：首页（index）、语言选择页（change）、历史记录页（history）
- `window` 配置导航栏为紫色（`#4b3c96`），标题为"小度翻译"
- `tabBar` 配置底部导航栏，包含"翻译"和"历史"两个 tab，分别对应首页和历史页
- `change` 页面不在 tabBar 中，因为它通过 `wx.navigateTo` 从首页跳转进入

### 3. app.wxss — 全局样式

```css
@import "./assets/iconfont/iconfont.wxss";
```

全局样式文件仅引入了 iconfont 图标字体样式，使所有页面都可以使用图标字体。

---

## 二、首页 — index（翻译页）

### 1. index.wxml — 页面结构

```xml
<!-- 最外层容器 -->
<view class="container">
  <!-- 目标语言的选择 -->
  <view class="lanSelectContainer">
    <text bindtap="goToChange">翻译成英语</text>
    <text class="iconfont icon-down"></text>
  </view>
  <!-- 文本输入框 -->
  <view class="tranlateTxt">
    <textarea
      placeholder="请输入您要翻译的文本"
    ></textarea>
  </view>
  <!-- 翻译结果 -->
  <view class="resultTxt">
    <view>译文</view>
    <view class="content">Hello</view>
  </view>
</view>
```

**要点说明：**
- 页面分为三个区域：语言选择栏、文本输入区、翻译结果区
- 语言选择栏通过 `bindtap="goToChange"` 绑定点击事件，点击后跳转到语言选择页
- 使用了 iconfont 的 `icon-down` 图标表示下拉箭头
- `textarea` 用于用户输入要翻译的文本
- 翻译结果区域目前是静态文本，后续课程会改为动态数据

### 2. index.js — 页面逻辑

```javascript
const app = getApp()

Page({
  data: {

  },
  // 跳转到选择语言
  goToChange(){
    wx.navigateTo({
      url: '/pages/change/change',
    })
  }
})
```

**要点说明：**
- 当前阶段 `data` 为空，后续课程会补充翻译相关的数据
- `goToChange` 方法使用 `wx.navigateTo` 跳转到语言选择页面，将新页面推入页面栈

### 3. index.wxss — 页面样式

```css
.lanSelectContainer{
  width: 100%;
  height: 80rpx;
  background-color: #f7f7fa;
  text-indent: 20rpx;
  line-height: 80rpx;
  font-size: 30rpx;
  color: #8995a1;
}

.lanSelectContainer .icon-down{
  font-size: 30rpx;
}

.tranlateTxt{
  width: 100%;
  height: 400rpx;
  border-bottom: 1rpx solid #8995a1;
  font-size: 32rpx;
}

.tranlateTxt textarea{
  margin: 30rpx 20rpx;
}

.resultTxt{
  font-size: 30rpx;
  color: #8995a1;
  padding: 30rpx;
}
.content{
  margin-top: 10rpx;
  color: black;
}
```

**要点说明：**
- 使用 `rpx` 作为尺寸单位，确保在不同分辨率屏幕上的适配
- 语言选择栏使用浅灰背景（`#f7f7fa`），灰色文字（`#8995a1`）
- 文本输入区域高度为 400rpx，底部有灰色分割线
- 翻译结果区域有内边距，内容文字为黑色

---

## 三、语言选择页 — change

### 1. change.wxml — 页面结构

```xml
<!-- 一项一项的语言 -->
<view
  class="container"
  wx:for="{{lanList}}"
  wx:key="this"
  data-id="{{index}}"
>
  <view
    wx:if="{{curLanIndex === index}}"
  >
    {{item.chs}}
    <text class="select iconfont icon-duihao"></text>
  </view>
  <view wx:else>
    {{item.chs}}
  </view>
</view>
```

**要点说明：**
- 使用 `wx:for` 列表渲染遍历 `lanList`，显示所有可选语言
- 使用 `wx:if` / `wx:else` 条件渲染：当前选中的语言会额外显示一个勾选图标（`icon-duihao`）
- 通过 `data-id` 自定义属性存储每一项的索引，方便后续事件处理

### 2. change.js — 页面逻辑

```javascript
const app = getApp(); // 获取实例
Page({
  data: {
    lanList : app.globalData.lanList, // 从全局获取所有的语言
    curLanIndex : app.globalData.curLan.index // 当前语言的索引
  },
})
```

**要点说明：**
- 从 `app.globalData` 中获取语言列表和当前语言索引，初始化页面数据
- 当前阶段还未添加语言选择的点击事件，下一节课会完善

### 3. change.wxss — 页面样式

```css
.container{
  width: 100%;
  height: 80rpx;
  border-bottom: 1px solid #eee;
  line-height: 80rpx;
  font-size: 30rpx;
  color: #8995a1;
  text-indent: 30rpx;
}

.select{
  float: right;
  margin-right: 40rpx;
}
```

**要点说明：**
- 每一行语言项高度 80rpx，底部有浅色分割线
- 勾选图标通过 `float: right` 定位到右侧

---

## 四、历史记录页 — history

### 1. history.wxml — 页面结构

```xml
<scroll-view class="container">
  <!-- 头部 -->
  <view class="header">
    <text>翻译历史</text>
    <text class="iconfont icon-close">清除历史记录</text>
  </view>
  <!-- 历史记录 -->
  <view class="list">
    <view class="item">
      <view>你好</view>
      <view>Hello</view>
    </view>
    <view class="item">
      <view>你好</view>
      <view>Hello</view>
    </view>
    <view class="item">
      <view>你好</view>
      <view>Hello</view>
    </view>
  </view>
</scroll-view>
```

**要点说明：**
- 外层使用 `scroll-view` 可滚动容器
- 头部包含标题"翻译历史"和"清除历史记录"按钮（使用 iconfont 的关闭图标）
- 历史记录列表目前为静态数据，后续会改为动态渲染

### 2. history.js — 页面逻辑

```javascript
// pages/history/history.js
Page({
  data: {},
  onLoad(options) {},
  onReady() {},
  onShow() {},
  onHide() {},
  onUnload() {},
  onPullDownRefresh() {},
  onReachBottom() {},
  onShareAppMessage() {}
})
```

**要点说明：**
- 当前阶段为微信开发者工具生成的默认模板，包含所有生命周期钩子函数的空实现
- 下一节课会补充历史记录的加载和清除功能

### 3. history.wxss — 页面样式

```css
.container{
  font-size: 30rpx;
  color: #aaa;
}

.header{
  display: flex;
  justify-content: space-between;
  height: 100rpx;
  line-height: 100rpx;
  padding: 20rpx;
}

.header text{
  margin: 0 20rpx;
}

.header .icon-close{
  font-size: 30rpx;
}

.item{
  margin: 30rpx;
}

.item view{
  margin: 5rpx 0;
}

.item view:nth-child(2){
  color: black;
}
```

**要点说明：**
- 头部使用 flex 布局，标题和清除按钮分布在两端
- 每条历史记录项包含原文和译文两行，译文（第二个 view）为黑色文字

---

## 五、本节课小结

本节课完成了翻译小程序的项目骨架搭建：

1. **全局配置**：通过 `app.js` 和 `app.json` 配置了全局数据、页面路由、导航栏样式和底部 tabBar
2. **三个页面结构**：
   - 首页（index）：包含语言选择栏、文本输入区、翻译结果区
   - 语言选择页（change）：使用列表渲染展示所有可选语言
   - 历史记录页（history）：展示翻译历史列表和清除按钮
3. **样式设计**：使用 rpx 单位适配不同屏幕，整体采用紫色主题色调
4. **图标字体**：通过 iconfont 引入图标，提升界面美观度

下一节课将在此基础上实现翻译功能的核心逻辑。
