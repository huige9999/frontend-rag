# 18. Repository与QueryBuilder处理

## 一、对于ORM的理解

面向对象编程和关系型数据库，都是目前最流行的技术，但是它们的模型是不一样的。

面向对象编程把所有实体看成对象（object），关系型数据库则是采用实体之间的关系（relation）连接数据。很早就有人提出，关系也可以用对象表达，这样的话，就能使用面向对象编程，来操作关系型数据库。

**简单说，ORM 就是通过实例对象的语法，完成关系型数据库的操作的技术，是"对象-关系映射"（Object/Relational Mapping） 的缩写。**

ORM 把数据库映射成对象

- 数据库的表（table） --> 类（class）
- 记录（record，行数据）--> 对象（object）
- 字段（field）--> 对象的属性（attribute）

举例来说，下面是一行 SQL 语句。

```sql
SELECT id, first_name, last_name, phone, birth_date, sex
FROM persons 
WHERE id = 10
```

如果程序直接操作SQL，大致语法如下：

```typescript
res = db.execSql(sql);
name = res[0]["FIRST_NAME"];
```

如果改成 ORM 的写法大致如下：

```typescript
p = Person.get(10);
name = p.first_name;
```

一比较就可以发现，ORM 使用对象，封装了数据库操作，因此可以不碰 SQL 语言。开发者只使用面向对象编程，与数据对象直接交互，不用关心底层数据库。

总结起来，ORM 有下面这些优点。

> - 数据模型都在一个地方定义，更容易更新和维护，也利于重用代码。
> - ORM 有现成的工具，很多功能都可以自动完成，比如数据消毒、预处理、事务等等。
> - 它迫使你使用 MVC 架构，ORM 就是天然的 Model，最终使代码更清晰。
> - 基于 ORM 的业务代码比较简单，代码量少，语义性好，容易理解。
> - 你不必编写性能不佳的 SQL。

但是，ORM 也有很突出的缺点。

> - ORM 库不是轻量级工具，需要花精力学习和设置。
> - 对于复杂的查询，ORM 要么是无法表达，要么是性能不如原生的 SQL。
> - ORM 抽象掉了数据库层，开发者无法了解底层的数据库操作，也无法定制一些特殊的 SQL。

## 二、TypeORM

[**TypeORM**](https://typeorm.io/)([中文文档](https://www.typeorm.org/))是一个[ORM](https://en.wikipedia.org/wiki/Object-relational_mapping)，可以在 NodeJS、浏览器、Cordova、PhoneGap、Ionic、React Native、NativeScript、Expo 和 Electron 平台上运行，并且可以与 TypeScript 和 JavaScript（ES5、ES6、ES7、ES8）一起使用。

### 直接开始

直接运行命令创建TypeORM项目

```typescript
npx typeorm init --name typeorm-test --database mysql2
```

> 没有安装TypeORM包的话，使用npx命令时，系统会自动检测是否已经安装这个包。选择"y"后，脚本将自动安装相关的依赖包。
>
> 不过项目并不会自动帮你安装mysql的依赖包，所以项目创建好之后，还需在项目中安装mysql2的相关依赖
>
> ```shell
> npm i mysql2 --save
> ```

### 创建模型及实体

要处理数据库，首先当然要创建表，在TypeORM中，我们通过定义实体来告诉他如何创建一个数据表，比如，我们要创建一个用户表，就需要定义用户的实体模型

```typescript
@Entity()
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ length: 100 })
  name: string;

  @Column({ length: 100 })
  nickname: string;

  @Column()
  age: number;

  @Column({ length: 11 })
  phone: string;

  @Column("text")
  desc: string;

  @Column("double", { default: 0 })
  other: number;
}
```

我们仅仅定义一个User模型并不够，还需要使用装饰器`@Entity`将其定义为一个实体

用于装饰列的装饰器包括 `@Column`、`@PrimaryGeneratedColumn`和`@PrimaryColumn`。`@PrimaryColumn`用于定义普通主键列，而`@PrimaryGeneratedColumn`用于定义自增主键列。

string类型默认映射为数据库中的`varchar(255)`，`number`类型默认映射为`int`整数类型。如果我们希望某个字段是文本(Text)类型或者(Double)类型，可以通过`@Column`装饰器指定

### 连接数据库

为了和数据库进行交互，我们需要设置一个数据源(DataSource)。数据源中包含了与数据库连接相关的配置。通过这些配置，我们可以建立与数据库的初始连接或者连接池。

```typescript
export const AppDataSource = new DataSource({
  type: "mysql",
  host: "localhost",
  port: 3306,
  username: "root",
  password: "123456",
  database: "typeorm_test",
  synchronize: true,
  logging: true,
  entities: [User],
  migrations: [],
  subscribers: [],
});
```

- **type** ：表示数据库的类型，TypeORM 不只支持 MySQL 还支持 postgres、oracle、sqllite 等数据库。
- **database**：表示你需要连接的数据库名，mysql 是可以有多个 `database` (`schema`)的。**注意：**数据库并不能帮你创建，需要自己先创建数据库
- **host**、**port** ：表示数据库服务器的主机和端口号
- **user**、**password**：表示登录数据库的用户名和密码
- **synchronize** ：表示根据同步建表，简单来说就是当 database 里没有和 Entity 对应的表的时候，会自动生成建表 sql 语句并执行
- **logging**：启动日志记录，会打印生成的 sql 语句
- **entities** ：指定有哪些和数据库的表对应的 Entity，也可以通过路径的方式指定，也可以用通配符，比如：`entities: ['./**/entity/*.ts']`

还有一些其他的配置：

- **migrations** ：修改表结构之类的 sql
- **subscribers**：Entity 生命周期的订阅者，比如 insert、update、remove 前后，可以加入一些逻辑
- **poolSize**：指定数据库连接池中连接的最大数量
- **connectorPackage**：指定驱动包

创建**DataSource**实例之后，需要调用该实例的**initialize**方法来创建数据库连接。

### 使用实体管理器操作数据库

一旦数据库连接成功，我们就可以使用实体管理器（manager）来执行数据库操作：

```typescript
AppDataSource.initialize()
  .then(async () => {
    const user1 = new User();
    user1.name = "jack";
    user1.nickname = "张三";
    user1.phone = "13311111111";
    user1.age = 25;
    user1.desc = "This is a desc1";

    // 插入数据
    await AppDataSource.manager.save(user1);

    // 查询数据
    const users = await AppDataSource.manager.find(User);

    // 更新数据
    user1.name = "tom";
    await AppDataSource.manager.save(user1);

    // 条件查询
    const user = await AppDataSource.manager.findOneBy(User, { id: 2 });
  })
  .catch((error) => console.log(error));
```

### 使用Repository操作CRUD

除了使用实体管理器(EntityManager)来管理实体外，我们还可以使用存储库(Repository)来管理每个实体：

```typescript
async function main() { 
  await AppDataSource.initialize();
  const userRepository = AppDataSource.getRepository(User);
  
  // 新增
  const user1 = new User();
  user1.name = "jack";
  await userRepository.save(user1);

  // 查询全部
  const users = await userRepository.find();

  // 条件查询
  const user = await userRepository.findOneBy({ id: 2 });

  // 更新
  user.name = "tom";
  await userRepository.save(user);

  // 删除
  const userDelete = await userRepository.findOneBy({ id: 2 });
  await userRepository.delete(userDelete);
}
main();
```

### 使用QueryBuilder操作CRUD

**QueryBuilder**是TypeORM中最强大的功能之一，它提供了更加底层的数据库查询操作，灵活度高，支持组合更复杂的SQL查询语句，比如多条件查询、多表查询等等。

创建**QueryBuilder**的方式有很多，可以通过**DataSource创建**，也可以通过**Repository创建**，还能通过**EntityManager创建**

```typescript
// DataSource创建
const user = await AppDataSource.createQueryBuilder()
  .select("user")
  .from(User, "user")
  .where("user.id = :id", { id: 1 })
  .getOne();
```

```typescript
// Repository创建
const user = await AppDataSource.getRepository(User)
  .createQueryBuilder("user")
  .where("user.id = :id", { id: 1 })
  .getOne();
```

```typescript
// EntityManager创建
const user = await AppDataSource.manager
  .createQueryBuilder(User, "user")
  .where("user.id = :id", { id: 1 })
  .getOne();
```

使用哪种方式创建都可以。

**新增数据：**

```typescript
const user = new User();
user.name = "lily";

const queryBuilder = AppDataSource.createQueryBuilder();
const result = await queryBuilder
  .insert()
  .into(User)
  .values(user)
  .execute();
```

上面代码使用insert和into方法进行插入操作，语法上接近原始的SQL语句，**value方法中可以传入对象或者数组**，如果要插入多条记录，就可以选择传入数组。最后调用**execute**方法执行语句。

**查询数据：**

```typescript
// 查询实体对象（与代码实体一一对应）
const saveUsers = await queryBuilder.select("u").from(User, "u").getMany();

// 查询数据库原始字段内容（使用getRawMany）
const saveUsers = await queryBuilder.select().from(User, "u").getRawMany();
```

**getMany**方法查询出来的记录和代码的实体对象一一对应。如果数据库的字段和实体字段不一致，只想查询数据库中的原始字段，可以使用**getRawMany**。

**查询并更新：**

```typescript
const userUpdate = await queryBuilder.select("u").from(User, "u")
  .where("u.id = :id", { id: 1 }).getOne();

userUpdate.name = "lucy";
const resultUpdate = await queryBuilder
  .update(User)
  .set(userUpdate)
  .where("id = :id", { id: 1 })
  .execute();
```

**删除数据：**

```typescript
const resultDelete = await queryBuilder
  .delete()
  .from(User)
  .where("id = :id", { id: 1 })
  .execute();
```

## 三、TypeORM处理多表关系

### 一对一关系

以用户表和身份证表为例，使用 `@OneToOne` 装饰器来建立关联关系，同时用 `@JoinColumn()` 装饰器来定义外键列：

```typescript
@Entity()
export class IdCard { 
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ length: 18 })
  cardNo: string;

  @OneToOne(() => User)
  @JoinColumn()
  user: User;
} 
```

**注意：**添加了实体，就需要将实体添加到DataSource配置的entities中。

插入关联数据的关键点是**要确保他们关联起来**：

```typescript
const user = new User();
user.name = "hmm";

const idCard = new IdCard();
idCard.cardNo = "123456789012345678";

// 关联两个实体
idCard.user = user;

await userRepository.save(user);
await idCardRepository.save(idCard);
```

查询关联数据时需要**设置关联（relations）**：

```typescript
const idCards = await idCardRepository.find({
  relations: { user: true },
});
```

两个实体的关系默认是**单向的**，只在IdCard实体中持有User实体的外键列，IdCard有权访问User，但User并不知道IdCard的存在。

#### 双向关联

为了让User实体能够访问到IdCard实体，需要修改实体类建立双向关联：

```typescript
// IdCard实体
@OneToOne(() => User, (user) => user.card)
@JoinColumn()
user: User;

// User实体
@OneToOne(() => IdCard, (idCard) => idCard.user)
card: IdCard;
```

#### 级联处理

在实体类中加上级联处理（`cascade: true`），就可以使用一个存储库直接保存关联数据：

```typescript
// User实体中设置级联
@OneToOne(() => IdCard, (idCard) => idCard.user, {
  cascade: true,
})
card: IdCard;
```

设置级联后，只需要通过User库就能直接保存关联的IdCard信息：

```typescript
user.card = idCard;
await userRepository.save(user);  // 自动级联保存idCard
```

更新时同样可以级联操作。

### 一对多/多对一关系

以部门和员工为例（一个部门有多个员工，一个员工只属于一个部门）：

```typescript
// 部门实体
@Entity()
export class Department { 
  @OneToMany(() => Employee, employee => employee.department, {
    cascade: true
  })
  employees: Employee[];
}

// 员工实体
@Entity()
export class Employee {
  @ManyToOne(() => Department, department => department.employees)
  @JoinColumn() 
  department: Department;
}
```

- `@OneToMany` 并不能单独使用，必须与 `@ManyToOne` 装饰器配对使用
- 外键列设置在"多"的一方（Employee）
- 由于是"多"，employees 是一个数组

插入数据：

```typescript
depart.employees = [emp1, emp2];
await departmentRepository.save(depart);
```

### 多对多关系

以订单和商品为例，使用 `@JoinTable()` 来生成中间表：

```typescript
// 订单实体
@Entity()
export class Order {
  @ManyToMany(() => Product, product => product.orders, {
    cascade: true
  })
  products: Product[];
}

// 商品实体
@Entity()
export class Product {
  @ManyToMany(() => Order, order => order.products)
  @JoinTable()
  orders: Order[];
}
```

TypeORM会自动创建一个中间表来管理多对多关系。

还可以自定义中间表的名称和列名：

```typescript
@JoinTable({
  name: 'order_products',
  joinColumn: {
    name: 'order_id',
    referencedColumnName: 'id',
  },
  inverseJoinColumn: {
    name: 'product_id',
    referencedColumnName: 'id',
  },
})
```

## 四、在Nest中使用TypeORM

掌握了TypeORM的基本用法之后，在Nest中集成TypeORM就特别简单了。

### 项目初始化

```shell
nest n nest-typeorm-mysql -p -pnpm
nest g res user
pnpm add typeorm mysql2 @nestjs/typeorm -s
```

### 配置数据库连接

在`app.module.ts`文件中引入TypeORM模块：

```typescript
@Module({
  imports: [
    TypeOrmModule.forRoot({
      type: 'mysql',
      host: 'localhost',
      username: 'root',
      port: 3306,
      password: '123456',
      database: 'nest_typeorm',
      entities: [__dirname + '/**/*.entity{.ts,.js}'],
      synchronize: true,
      timezone: 'Z',
    }),
    UserModule,
  ],
})
export class AppModule {}
```

> **注意：**`synchronize: true` 仅方便开发使用，生产环境中应禁用，避免随意改变数据库结构。

### EntityManager操作实体

通过Nest直接注入EntityManager来操作实体：

```typescript
@Injectable()
export class UserService {
  @InjectEntityManager()
  private entityManager: EntityManager;

  async create(createUserDto: CreateUserDto) {
    return await this.entityManager.save(User, createUserDto);
  }

  async findAll() {
    return await this.entityManager.find(User);
  }

  async findOne(id: number) {
    return await this.entityManager.findOneBy(User, { id });
  }

  async update(id: number, updateUserDto: UpdateUserDto) {
    return await this.entityManager.save(User, { id, ...updateUserDto });
  }

  async remove(id: number) {
    return await this.entityManager.delete(User, { id });
  }
}
```

### Repository操作实体

使用`Repository`操作实体，和`EntityManager`几乎一致，唯一区别是**不需要每次操作都传入实体对象**：

```typescript
@Injectable()
export class UserService {
  @InjectRepository(User)
  private userRepository: Repository<User>;

  async create(createUserDto: CreateUserDto) {
    return await this.userRepository.save(createUserDto);
  }

  async findAll() {
    return await this.userRepository.find();
  }

  async findOne(id: number) {
    return await this.userRepository.findOneBy({ id });
  }

  async update(id: number, updateUserDto: UpdateUserDto) {
    return await this.userRepository.save({ id, ...updateUserDto });
  }

  async remove(id: number) {
    return await this.userRepository.delete({ id });
  }
}
```

**注意：**由于Nest的**依赖注入作用域限定在模块级别**，因此Repository需要在模块中注册：

```typescript
@Module({
  imports: [TypeOrmModule.forFeature([User])],
  controllers: [UserController],
  providers: [UserService],
})
export class UserModule {}
```

### QueryBuilder操作实体

当需要更复杂的SQL（如使用`or`条件连接）时，可以使用QueryBuilder：

```typescript
@Injectable()
export class UserService {
  @InjectRepository(User)
  private userRepository: Repository<User>;

  async create(createUserDto: CreateUserDto) {
    const user = await this.userRepository
      .createQueryBuilder()
      .insert()
      .into(User)
      .values(createUserDto)
      .execute();
    return user;
  }

  async findAll(name: string, sex: string) {
    return this.userRepository
      .createQueryBuilder('u')
      .where('u.name = :name or u.sex = :sex', { name, sex })
      .getMany();
  }
}
```

Repository的`find`方法中`where`条件的连接方式是`and`，如果希望使用`or`进行连接，就可以使用QueryBuilder。

## 关键代码

### 实体定义 - User Entity

对应文件：`代码/typeorm-test/src/entity/User.ts`

```typescript
@Entity()
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ name: "u_name", length: 100 })
  name: string;

  @Column({ name: "u_age" })
  age: number;

  @Column({ length: 11 })
  phone: string;

  @Column("text")
  desc: string;

  @Column("double", { default: 0 })
  other: number;
}
```

说明：通过 `@Column` 的 `name` 属性可以指定数据库字段名与实体属性名的映射关系。

### 数据源配置

对应文件：`代码/typeorm-test/src/data-source.ts`

```typescript
export const AppDataSource = new DataSource({
  type: "mysql",
  database: "typeorm_test",
  host: "localhost",
  port: 3306,
  username: "root",
  password: "123456",
  synchronize: true,
  logging: true,
  entities: [User],
});
```

### 三种 QueryBuilder 创建方式

对应文件：`代码/typeorm-test/src/index.ts`

```typescript
// 方式一：通过 DataSource 创建
const user = await AppDataSource.createQueryBuilder()
  .select("user").from(User, "user")
  .where("user.id=:id", { id: 1 }).getOne();

// 方式二：通过 Repository 创建
const user = await AppDataSource.getRepository(User)
  .createQueryBuilder("user")
  .where("user.id=:id", { id: 1 }).getOne();

// 方式三：通过 EntityManager 创建
const user = await AppDataSource.manager
  .createQueryBuilder(User, "user")
  .where("user.id=:id", { id: 1 }).getOne();
```

### QueryBuilder 增删改查核心操作

对应文件：`代码/typeorm-test/src/index.ts`

```typescript
// 新增
const queryBuilder = AppDataSource.createQueryBuilder();
const result = await queryBuilder.insert().into(User).values(user1).execute();

// 查询实体对象
const users = await queryBuilder.select("user").from(User, "user").getMany();

// 查询数据库原始字段
const users = await queryBuilder.select().from(User, "user").getRawMany();

// 更新
await queryBuilder.update(User).set(user).where("id=:id", { id: 7 }).execute();

// 删除
await queryBuilder.delete().from(User).where("id=:id", { id: 8 }).execute();
```

### Repository CRUD 操作

对应文件：`代码/typeorm-test/src/index.ts`

```typescript
const userRepository = AppDataSource.getRepository(User);

// 新增
await userRepository.save(user1);

// 查询全部
await userRepository.find();

// 条件查询
await userRepository.findOneBy({ id: 2 });

// 更新（先查后改再save）
user.name = 'Jack';
await userRepository.save(user);

// 删除
await userRepository.delete(userDelete);
```
