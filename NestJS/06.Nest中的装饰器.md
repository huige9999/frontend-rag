# 06. Nest中的装饰器

## 课件内容

### 装饰器类型概述

TS中的常见的装饰器类型有5种，包括类装饰器、方法装饰器、属性装饰器、参数装饰器和访问器装饰器。

在Nest中实现了前4种装饰器：

- **类装饰器**：@Controller、@Injectable、@Module、@UseInterceptors
- **方法装饰器**：@Get、@Post、@Patch、@Delete、@UseInterceptors
- **属性装饰器**：@IsNotEmpty、@IsString、@IsNumber
- **参数装饰器**：@Body、@Param、@Query

当然，常用的装饰器远远不只我列举的这些，这里只是简单给大家提及一下最常用的，其他的一些大家可以慢慢认识。

### 常用装饰器说明

- `@Controller()`：用于装饰控制器类，使之能够管理应用中的路由程序，并通过设置路由路径前缀来模块化管理路由
- `@Injectable()`：装饰后成为服务提供者(provider)，可以被其他对象进行依赖注入
- `@Module()`：模块装饰器，用于在Nest中划分功能模块并限制依赖注入的范围
- `@UseInterceptors()`：用于绑定拦截器，也可以作用在类方法上，AOP相关的装饰器都与之类似，比如@UseFilters()、@UsePipes()等等
- `@Global()`：声明全局模块
- `@Get`、`@Post`、`@Put`、`@Delete`、`@Patch`、`@Options`、`@Head`：声明 get、post、put、delete、patch、options、head 的请求方式

### HTTP数据传输的5种方式

对于前端来说，后端主要是提供 http 接口来传输数据，而这种数据传输的方式主要有 5 种：

- **url param**：可以把参数写在 url 中，比如：`http://127.0.0.1:3000/person/123`
- **query**：通过 url 中`?`后面的用`&`分隔的字符串传递数据。比如：`http://127.0.0.1:3000/person?name=jack&age=19`
- **form-urlencoded**：直接用form表单提交数据的方式，`content-type` 是 `application/x-www-form-urlencoded`，数据放在请求体中
- **json**：传输 json 数据，`content type` 为 `application/json`
- **form-data**：传输文件的方式，需要指定`content type` 为 `multipart/form-data`，然后指定 `boundary` 分割线

对于`url param`和`query`其实就是`GET`方式的请求，只是url处理不一样。

#### url param 与 query 示例

```typescript
@Controller('user')
export class UserController {
  constructor(private readonly userService: UserService) {}

  @Get('find')
  query(@Query('name') name: string, @Query('age') age: number) {
    return this.userService.query(name, age);
  }

  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.userService.findOne(+id);
  }
}
```

> **注意**：`find`的路由要放到 :id 的路由前面，**Nest 是从上往下匹配**的，如果放在后面，就会先匹配到 `:id` 路由

#### form-urlencoded 与 JSON

用 Nest 接收的话，使用 `@Body` 装饰器，Nest 会解析请求体，然后注入到 `dto` 中。

`dto` 就是 `data transfer object`，就是用于封装传输的数据的对象。

```typescript
// create-user.dto.ts
export class CreateUserDto {
  name: string;
  age: number;
}

// user.controller.ts
@Controller('user')
export class UserController {
  constructor(private readonly userService: UserService) {}

  @Post()
  create(@Body() createUserDto: CreateUserDto) {
    return this.userService.create(createUserDto);
  }
}
```

JSON方式无非就是前端传输的时候，选择的`content-type` 为 `application/json`，内容会以 JSON 的方式传输。后端代码同样使用`@Body` 来接收，在Nest中的处理和`form-urlencoded`是一样的。

#### form-data

`form-data`的方式由于还需要用到其他的一些内容，后面通过例子一并讲解。

### 其他参数装饰器

除了这些常用的参数装饰器之外，还有其他的参数装饰器，比如，如果我们想拿到IP地址，拿到请求头信息这些内容，就可以直接通过参数装饰器获取：

```typescript
@Get('other')
other(
  @Ip() ip: string,
  @Headers() headers: Record<string, any>,
  @Req() request: Request,
) {
  console.log(ip);
  console.log(headers);
  console.log(request.url);
  return 'other';
}
```

上面通过`@Ip()`、`@Headers()`装饰器就获取IP地址和头信息，通过`@Req()`直接获取request请求对象。在Nest中`@Req`和`@Request`是一致的。

### 关于 @Res 装饰器

既然能获取请求对象，那么响应对象`Response`也能获取，可以通过注解`@Res`或者`@Response`，不过注意一点，如果手动加入了响应对象，那么就必须自己通过响应对象处理返回信息：

```typescript
@Get('other')
other(
  @Ip() ip: string,
  @Headers() headers: Record<string, any>,
  @Req() request: Request,
  @Res() response: Response,
) {
  console.log(ip);
  console.log(headers);
  console.log(request.url);
  // 必须手动处理响应，不能再用 return
  response.end('other');
}
```

> **注意**：这里的`Request`、`Response`对象，需要从`express`中导入：`import { Request, Response } from 'express';`

---

## 关键代码

### 1. UserController — 参数装饰器综合演示

文件：`代码/cli-test/src/user/user.controller.ts`

展示了 `@Query`、`@Param`、`@Body`、`@Ip`、`@Headers`、`@Req`、`@Res` 等参数装饰器的实际使用方式，同时展示了 `@Inject` 属性注入的写法（替代构造函数注入）：

```typescript
@Controller('user')
export class UserController {
  @Inject('user_service')
  private userService: UserService;

  // query 方式：@Query 获取查询参数
  @Get('find')
  query(@Query('name') name: string, @Query('age') age: number) {
    return this.userService.query(name, age);
  }

  // url param 方式：@Param 获取路径参数
  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.userService.findOne(+id);
  }

  // 请求体方式：@Body 获取请求体数据
  @Post('add')
  add(@Body() user: CreateUserDto) {
    return this.userService.add(user);
  }

  // 其他参数装饰器：@Ip、@Headers、@Req、@Res
  @Get('other')
  other(
    @Ip() ip: string,
    @Headers('content-type') headers: string,
    @Req() req: Request,
    @Res() res: Response,
  ) {
    res.end('other'); // 使用 @Res 后必须手动处理响应
  }
}
```

### 2. PersonController — CRUD 路由装饰器演示

文件：`代码/cli-test/src/person/person.controller.ts`

展示了 `@Post`、`@Get`、`@Patch`、`@Delete` 配合 `@Body`、`@Param` 完成标准 CRUD 操作：

```typescript
@Controller('person')
export class PersonController {
  @Post()
  create(@Body() createPersonDto: CreatePersonDto) { ... }

  @Get('list')
  findAll() { ... }

  @Get('get/:id')
  findOne(@Param('id') id: string) { ... }

  @Patch(':id')
  update(@Param('id') id: string, @Body() updatePersonDto: UpdatePersonDto) { ... }

  @Delete(':id')
  remove(@Param('id') id: string) { ... }
}
```

### 3. AppModule — @Module 与自定义 Provider

文件：`代码/cli-test/src/app.module.ts`

展示了 `@Module` 装饰器中 providers 的三种注册方式：`useClass`、`useValue`、`useFactory`：

```typescript
@Module({
  imports: [UserModule, PersonModule],
  controllers: [AppController],
  providers: [
    { provide: AppService, useClass: AppService },          // useClass
    { provide: 'car', useValue: { brand: 'BYD', price: 100000 } },  // useValue
    { provide: 'random', useFactory: createRandomFactory, inject: ['car', AppService] }, // useFactory
  ],
})
export class AppModule {}
```

### 4. DTO 数据传输对象

文件：`代码/cli-test/src/user/dto/create-user.dto.ts`

DTO 用于封装通过 `@Body` 接收的请求数据：

```typescript
export class CreateUserDto {
  name: string;
  password: string;
}
```
