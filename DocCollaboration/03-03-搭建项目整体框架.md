# 搭建项目整体框架

> 本节课主要通过代码实现来学习相关知识。本节使用 Next.js 搭建项目的前端整体框架，包括根布局、主布局、页面路由、侧边栏和导航栏等核心组件。

## 关键代码

### app/layout.tsx — 根布局

应用的根布局文件，配置页面元数据、主题提供者和全局样式。

```tsx
import "@/styles/globals.css";
import { Metadata } from "next";

import { Providers } from "./providers";

import { siteConfig } from "@/config/site";

export const metadata: Metadata = {
  title: {
    default: siteConfig.name,
    template: `%s - ${siteConfig.name}`,
  },
  description: siteConfig.description,
  icons: {
    icon: "/logo.png",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html suppressHydrationWarning lang="en">
      <head />
      <body>
        <Providers themeProps={{ attribute: "class", defaultTheme: "dark" }}>
          {children}
        </Providers>
      </body>
    </html>
  );
}
```

### app/(main)/layout.tsx — 主布局

主页面布局，包含顶部导航栏（Navbar）和侧边栏（Sidebar），编辑器页面时隐藏侧边栏。

```tsx
"use client";

import { usePathname } from "next/navigation";

import { Navbar } from "@/components/navbar";
import { Sidebar } from "@/components/sidebar";

export default function MainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const pathname = usePathname();

  // 判断当前路径是否为编辑器页面
  const isEditorPage = pathname.startsWith("/editor/");

  return (
    <div className="h-screen flex flex-col">
      <div className="w-full">
        <Navbar />
      </div>

      <main className="flex flex-1 overflow-hidden">
        {!isEditorPage && <Sidebar />}
        <section className="flex-1 overflow-y-auto p-6 bg-white dark:bg-black">
          {children}
        </section>
      </main>
    </div>
  );
}
```

### app/(main)/page.tsx — 首页

主布局下的首页组件，显示简单的欢迎内容。

```tsx
"use client";
export default function Home() {
  return (
    <div className="p-6 bg-white dark:bg-black">
      <h1 className="text-2xl font-semibold">首页</h1>
      <p className="mt-4">首页页面</p>
    </div>
  );
}
```

### app/login/page.tsx — 登录页

登录页面占位组件，后续课程将完善表单逻辑。

```tsx
export default function LoginPage() {
  return (
    <div className="p-6 bg-white dark:bg-black">
      <h1 className="text-2xl font-semibold">登录</h1>
      <p className="mt-4">登录页面</p>
    </div>
  );
}
```

### app/register/page.tsx — 注册页

注册页面占位组件，后续课程将完善表单逻辑。

```tsx
export default function RegisterPage() {
  return (
    <div className="p-6 bg-white dark:bg-black">
      <h1 className="text-2xl font-semibold">注册</h1>
      <p className="mt-4">注册页面</p>
    </div>
  );
}
```

### components/sidebar.tsx — 侧边栏

根据当前路径动态显示侧边栏菜单项，支持图标映射和高亮当前选中项。

```tsx
"use client";

import NextLink from "next/link";
import { usePathname } from "next/navigation";
import {
  HomeIcon, UsersIcon, TrashIcon, DocumentTextIcon,
  EyeIcon, StarIcon, MapIcon, BookOpenIcon,
  KeyIcon, PhoneIcon, ArchiveBoxIcon,
} from "@heroicons/react/24/solid";

const iconMap = {
  home: HomeIcon,
  users: UsersIcon,
  trash: TrashIcon,
  "archive-box": ArchiveBoxIcon,
  "document-text": DocumentTextIcon,
  eye: EyeIcon,
  star: StarIcon,
  mail: MapIcon,
  "book-open": BookOpenIcon,
  keyboard: KeyIcon,
  phone: PhoneIcon,
};

import { getSidebarItemsByPath } from "@/tools/utils";

export const Sidebar = () => {
  const pathname = usePathname();
  const sidebarItems = getSidebarItemsByPath(pathname);

  return (
    <aside className="w-64 h-full bg-gray-100 dark:bg-gray-900 p-4 border-r border-gray-200 dark:border-gray-700 flex flex-col justify-between flex-shrink-0">
      <ul className="space-y-2">
        {sidebarItems.map((item) => {
          const Icon = iconMap[item.icon as keyof typeof iconMap] || HomeIcon;
          const isActive = pathname === item.href;

          return (
            <li key={item.href}>
              <NextLink
                className={`flex items-center gap-2 p-2 rounded-md text-gray-700 dark:text-gray-300 hover:text-primary ${isActive ? "bg-gray-300 dark:bg-gray-700" : ""}`}
                href={item.href}
              >
                <Icon className="h-5 w-5" />
                <span
                  className={`text-sm font-medium ${isActive ? "text-primary" : "text-gray-700"}`}
                >
                  {item.label}
                </span>
              </NextLink>
            </li>
          );
        })}
      </ul>
    </aside>
  );
};
```

### components/navbar.tsx — 导航栏

顶部导航栏组件，包含 Logo、菜单项、搜索框、主题切换和用户头像。

```tsx
"use client";
import {
  HomeIcon, UsersIcon, TrashIcon, QuestionMarkCircleIcon,
} from "@heroicons/react/24/solid";
import { Kbd } from "@heroui/kbd";
import { Input } from "@heroui/input";
import { Image } from "@heroui/image";
import { Avatar } from "@heroui/avatar";
import { link as linkStyles } from "@heroui/theme";
import NextLink from "next/link";
import { usePathname } from "next/navigation";
import clsx from "clsx";

import { siteConfig } from "@/config/site";
import { ThemeSwitch } from "@/components/theme-switch";
import { SearchIcon } from "@/components/icons";

const iconMap = {
  home: HomeIcon,
  users: UsersIcon,
  trash: TrashIcon,
  help: QuestionMarkCircleIcon,
};

export const Navbar = () => {
  const pathname = usePathname();

  const searchInput = (
    <Input
      aria-label="Search"
      classNames={{ inputWrapper: "bg-default-100", input: "text-sm" }}
      endContent={
        <Kbd className="hidden lg:inline-block" keys={["command"]}>K</Kbd>
      }
      labelPlacement="outside"
      placeholder="搜索文档..."
      startContent={
        <SearchIcon className="text-base text-default-400 pointer-events-none flex-shrink-0" />
      }
      type="search"
    />
  );

  return (
    <nav className="w-full border-b border-gray-200 dark:border-gray-700 px-4 sticky top-0 z-50 bg-white dark:bg-black pl-10 pr-10">
      <div className="flex items-center justify-between h-16">
        <div className="flex items-center gap-4">
          <NextLink className="flex items-center gap-1" href="/">
            <Image alt="logo" src="/logo.png" width={35} />
            <p className="font-bold text-inherit">渡一文档系统</p>
          </NextLink>
          <ul className="hidden lg:flex gap-4 ml-2 relative top-0.5">
            {siteConfig.navItems.map((item) => {
              const Icon = iconMap[item.icon as keyof typeof iconMap];
              const isActive = pathname === item.href;
              return (
                <li key={item.href}>
                  <NextLink
                    className={clsx(
                      linkStyles({ color: "foreground" }),
                      "flex items-center gap-0.5",
                      isActive && "text-primary font-medium",
                      "hover:text-primary",
                    )}
                    href={item.href}
                  >
                    <Icon className="h-4 w-4 text-gray-500" />
                    <p className="text-sm font-bold text-inherit text-gray-600">
                      {item.label}
                    </p>
                  </NextLink>
                </li>
              );
            })}
          </ul>
        </div>
        <div className="hidden sm:flex items-center gap-4">
          <div className="hidden sm:flex gap-2">
            <ThemeSwitch />
          </div>
          <div className="hidden lg:block">{searchInput}</div>
          <div className="hidden md:block">
            <Avatar src={"/avatar.gif"} />
          </div>
        </div>
      </div>
    </nav>
  );
};
```

### package.json — 项目依赖

项目使用了 React 19 + Next.js 15 + HeroUI 组件库，以及 Yjs、CodeMirror、Socket.IO 等协同编辑相关的依赖。

```json
{
  "name": "next-app-template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --ext .ts,.tsx -c .eslintrc.json --fix"
  },
  "dependencies": {
    "@codemirror/lang-markdown": "^6.3.2",
    "@codemirror/theme-one-dark": "^6.1.2",
    "@heroicons/react": "^2.2.0",
    "@heroui/alert": "^2.2.17",
    "@heroui/avatar": "^2.2.13",
    "@heroui/button": "2.2.17",
    "@heroui/code": "2.2.12",
    "@heroui/dropdown": "^2.3.17",
    "@heroui/image": "^2.2.10",
    "@heroui/input": "2.4.17",
    "@heroui/kbd": "2.2.13",
    "@heroui/link": "2.2.14",
    "@heroui/listbox": "2.3.16",
    "@heroui/modal": "^2.2.14",
    "@heroui/navbar": "2.2.15",
    "@heroui/snippet": "2.2.18",
    "@heroui/switch": "2.2.15",
    "@heroui/system": "2.4.13",
    "@heroui/table": "^2.2.16",
    "@heroui/theme": "2.4.13",
    "@hookform/resolvers": "^5.0.1",
    "@react-aria/ssr": "3.9.7",
    "@react-aria/visually-hidden": "3.8.21",
    "@types/js-cookie": "^3.0.6",
    "clsx": "2.1.1",
    "codemirror": "^6.0.1",
    "framer-motion": "11.13.1",
    "highlight.js": "^11.11.1",
    "intl-messageformat": "^10.5.0",
    "js-cookie": "^3.0.5",
    "marked": "^15.0.8",
    "next": "15.0.4",
    "next-themes": "^0.4.4",
    "quill": "^2.0.3",
    "quill-cursors": "^4.0.4",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-hook-form": "^7.55.0",
    "react-markdown": "^10.1.0",
    "rehype-highlight": "^7.0.2",
    "socket.io-client": "^4.8.1",
    "y-codemirror.next": "^0.3.5",
    "y-protocols": "^1.0.6",
    "y-quill": "^1.0.0",
    "y-websocket": "1.5.1",
    "yjs": "^13.6.24",
    "zod": "^3.24.2"
  }
}
```
